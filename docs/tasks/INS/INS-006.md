# INS-006 — Implement action workflow and immutable baseline capture

## Hierarchy and execution metadata

Domain: INS — Evidence-backed insights, actions and verified savings → Epic: E-INS-01 → Feature: F-INS-006 → User story: US-INS-006 → Implementation task: INS-006 → numbered micro-tasks below → validation → Definition of Done.

Milestone: M8. Owner persona: **Data science / FinOps / Frontend**. Implementation status: **NOT_STARTED**. Intended size: one focused implementation session; split into linked child tasks before coding if independent migrations/interfaces cannot fit safely.

## Objective / user story

As the responsible data science / finops / frontend, record human optimization decisions and their verification plan.

## Why this exists

This delivers **implement action workflow and immutable baseline capture** as a prerequisite-controlled capability. Its correctness is defined by the explicit oracle below, not by successful compilation. The [canonical contract](../../../docs/13-insights/intelligence.md) owns shared definitions; do not invent parallel formulas or schemas.

## Dependencies

- [INS-002](../INS/INS-002.md)
- [INS-003](../INS/INS-003.md)
- [INS-004](../INS/INS-004.md)
- [INS-005](../INS/INS-005.md)
- [CTL-004](../CTL/CTL-004.md)

Dependencies require accepted implementation evidence. A published prerequisite specification is insufficient.

## Inputs

Versioned synthetic fixtures and the accepted outputs of the explicit prerequisites below.

## Outputs and likely files/components

`apps/api/actions`, `apps/web/actions`, `data/contracts/action-baseline.json`. Add a task-specific fixture and validation target under `tests/spec/INS-006/`, plus redacted evidence under `docs/evidence/INS-006/<commit>/`. These are implementation destinations, not files already implemented by this specification.

## Detailed implementation steps

### Micro-task 1

Implement lifecycle transition guards, owner changes, comments, due dates and audit events.

### Micro-task 2

Freeze measurement population and baseline before intervention; record evidence of a human customer change without executing it.

### Micro-task 3

Reject validation without post-period evidence; test concurrent updates, withdrawn action and owner losing scope.

### Micro-task 4 — Prove and checkpoint

Run the validation below, record expected versus observed results and environment versions, update operational documentation, then checkpoint the task index with the exact next action. Do not mark DONE while a required live gate is unexecuted.

## Data model

Action owner/resource/insight/expected saving/implemented_at/status; baseline artifact reference and revision.

## API / interfaces

POST /v1/actions; transition and freeze-baseline commands require expected revision.

## Security and tenant isolation

Insight evidence and workflow use the same permission profile as costs. Revoked users cannot inspect linked queries or update actions. No customer DDL or arbitrary SQL execution is exposed.

For this task, test a foreign tenant identifier and an invalid or revoked scope at every input boundary it introduces. Internal infrastructure tasks instead prove environment/principal isolation and absence of public access. Test artifacts use synthetic tenant A/B data only.

## Idempotency / retries

Detection fingerprint plus input version deduplicates observations; action updates use revisions; baseline snapshots are immutable and restatements remain linked.

## Failure scenarios

Action performed before baseline; revoked owner; duplicate action; unverifiable change; edited historical baseline.

## Observability

Emit task/component, request/run/batch identifier, outcome, duration and a safe error class. Correlate tenant identifiers in protected logs, not unbounded public metric labels. Exclude credentials, full SQL and raw customer payloads. Record counts/checksums or configuration versions appropriate to this task; alert on repeated failures through the shared platform monitoring contract.

## UX behavior

| UX requirement | Behavior |
|---|---|
| Persona | FinOps / data engineer |
| Goal | record human optimization decisions and their verification plan. |
| Entry point | /optimize/actions |
| Happy path | Assign → plan baseline → record implementation → start verification. |
| Empty state | Create an action from a reviewed insight. |
| Loading state | Preserve selected scope and filters; show labelled skeletons and a cancellable progress indicator for asynchronous work. |
| Partial-data state | Explain missing baseline or verification coverage. |
| Error state | Keep user input; show a safe error code, retry action and support request ID. Never print SQL, tokens or internal hostnames. |
| Permission-denied state | Return a scoped 403 or non-enumerating 404; show an access-request explanation, with no foreign names, counts or totals. |
| Success state | Show the accepted version, UTC as-of time and completed outcome. Financial results include maturity, coverage and reconciliation status. |
| Drilldown behavior | Action → frozen baseline → intervention evidence → post-period observations. |
| Primary actions | Assign, plan, freeze, record implementation, cancel, verify. |
| Acceptance criteria | Implemented does not imply Validated; baseline hash remains unchanged after refresh; stale transition returns409 without mutation. |

## Testing and validation

**Deterministic oracle:** Implemented does not imply Validated; baseline hash remains unchanged after refresh; stale transition returns409 without mutation.

**Local:** the FND validation dispatcher must expose `make validate-task TASK=INS-006 ENV=local`. Implement the fixture before the behavior. Exercise the stated failure cases and retry the identical input; compare outputs with the oracle. Use unit/contract tests and local PostgreSQL/Redis integration where relevant. UI tasks add Playwright state assertions and keyboard checks; data tasks add dbt/Arrow contract assertions.

**AWS / Snowflake:** `make validate-task TASK=INS-006 ENV=staging` runs only against the isolated staging environment with short-lived identities. Run engines on isolated published Snowflake fixtures; prove no customer mutations, evidence scope isolation, replay determinism and baseline immutability.

**Required test selection:** include the relevant unit, contract, integration, tenant-isolation, security, failure/recovery, E2E/UX, performance and financial levels. Record `NOT_APPLICABLE` with a concrete reason for any level that does not exercise this task. Never replace a specified live behavior with a mock and report it as passed.

## Acceptance criteria

- [ ] Every micro-task output exists at the named destination and agrees with the canonical contract.
- [ ] The deterministic oracle above passes with recorded expected/actual output.
- [ ] Stated failure and replay/retry cases preserve the last accepted state.
- [ ] Unauthorized/environment-crossing cases are denied and audited without leakage.
- [ ] UX and diagnostics represent unknown, partial and failed outcomes accurately.

## Definition of Done

All acceptance criteria plus the [global Definition of Done](../../../DELIVERY_METHODOLOGY.md) pass. Attach the implementation commit, versions, local and required live evidence, reviewer and rollback result. Update the machine task index; specification authoring alone leaves status NOT_STARTED.

## Operational runbook and rollback impact

Add diagnosis, safe retry, rollback and escalation instructions for the failures listed in this task. Record the last safe durable state; never delete evidence to clear a failure.

## Documentation impact

Update the [canonical domain contract](../../../docs/13-insights/intelligence.md), affected API/schema examples and task evidence index. Record any changed external assumption in the research register and any architecture change through the ADR process. Preserve one canonical definition rather than copying shared rules into route handlers or other task files.
