# Operational runbook catalog

This is the canonical recovery design. OPS-010 replaces placeholders with actual Terraform outputs and tested commands during implementation. Commands use explicit staging/production profiles; never guess an account, tenant, bucket or Snowflake role. Record incident ID, actor, scope, current versions and last accepted checkpoint before mutation. Operational targets are defined in [operations](operations.md).

| Runbook / owner | Trigger and read-only diagnosis | Contain and recover | Verification and escalation |
|---|---|---|---|
| RB-01 WIF failure / Snowflake | Classify expired AWS session, wrong principal, disabled user, permission denied or network error; compare STS identity to registered IAM role and Snowflake WORKLOAD_IDENTITY mapping | Pause only affected account/source; refresh ephemeral credentials or have authorized customer correct reviewed trust/grants; rerun capability probe; resume existing checkpoint | Wrong-account probe must fail; authorized query succeeds; no key-pair fallback; escalate trust change to Security/customer admin |
| RB-02 Snowpipe lag / Data platform | Compare journal manifests, object counts, pipe status, COPY_HISTORY and receipt queue age; distinguish no notification, load error and missing acceptance | Preserve all keys; fix integration/KMS/schema with scoped owner; refresh only documented eligible files or replay under new immutable replay keys; do not rename originals or FORCE-load blindly | Every manifest file has correct receipt/checksum/count; accepted RAW dedup unchanged; watermark advances only through contiguous coverage |
| RB-03 Partial batch / Data platform | Inspect batch attempt, lease token, manifest and per-file receipts; a complete upload is not a committed batch | Fence failed owner; reuse exact immutable bytes only if contract permits; otherwise new attempt and manifest; leave incomplete attempt quarantined | No dbt consumption of incomplete batch; one accepted business revision; stale owner cannot commit |
| RB-04 Schema drift / Data engineer | Compare projected columns/types to source registry version; retain sanitized failure metadata and sample schema only | Quarantine incompatible source version; keep unaffected sources running; review official change, add new major contract and isolated RAW path, backfill impacted windows | Old/new fixtures pass; precision and nullability explicit; previous publication remains readable until new version accepted |
| RB-05 Coverage hole / Data platform | Compare source/journal/RAW/published interval maps and lease history, not only max timestamps | Plan bounded missing interval under fair backfill lane; apply source overlap; replay if journal exists; disclose unrecoverable retention gap | Contiguous coverage restored or explicit unavailable gap; duplicate/late records update once; no jump over hole |
| RB-06 Financial mismatch / FinOps | Pin ledger/reference/price/config versions; compute signed delta by currency/service/account; inspect latency, fees, adjustments and authority mapping | Mark reconciliation FAILED/WARNING; block closure; preserve existing closed statement; repair source/mapping/rate through versioned pipeline and re-evaluate | Golden270 vs271 remains−1 until supported correction; never add a balancing plug; material closed-period change creates restatement requiring finance approval |
| RB-07 Bad publication / Analytics engineer | Identify first bad candidate, failed invariant and affected partition dependencies | Stop affected publisher; repoint to prior compatible accepted revision using audited publication procedure; fix candidate and replay with new revision | All UI/API/export consumers read coherent prior version; closed statements unchanged; repaired candidate passes numerical and isolation gates |
| RB-08 Notification/report DLQ / Backend | Inspect logical event, destination version, attempt IDs and ambiguous provider receipt; do not print destination secret | Pause failed destination, reauthorize recipients, rotate through Secrets Manager if needed; replay same logical delivery with new attempt only after diagnosing permanent failure | One logical record; possible provider duplicate disclosed; secure links still enforce current scope; report checksums match accepted artifact |
| RB-09 Suspected tenant exposure / Security incident commander | Preserve access/audit evidence; determine affected identities, endpoints and time window without broad customer browsing | Disable vulnerable path/principal, invalidate permission epochs/caches/sessions, isolate artifacts; engage incident response and approved communication owner; retain evidence | Reproduce synthetic A/B attack and verify fix before reopening; incident owner determines contractual/regulatory notifications with qualified counsel |
| RB-10 Aurora restore / SRE | Identify requested recovery point, last restorable time, key access and current migration/outbox versions | Restore into new isolated cluster; keep outbound dispatch disabled; reapply deletion tombstones, validate RLS, reconcile external side effects; fence old writers then cut over | Compare known transaction markers, tenant denial, job ownership and outbox state; measure RPO/RTO; destroy old cluster only after approved retention period |
| RB-11 Snowflake recovery / Data platform | Select consistent recovery snapshot and subsequent accepted journal manifests; inspect checksums, publication map and grant manifest | Recreate identities/integrations/policies in isolated target; restore full retained facts/configs; replay post-snapshot batches; reapply deletions; switch read broker only after gates | Recover old180-day charge and new269 corrected fixture exactly; no lost required statements; prove A/B isolation and contiguous coverage |
| RB-12 Redis loss / Backend | Confirm cache unavailable and durable PG/Snowflake healthy; inspect cache stampede indicators | Fail open only to authorized database reads under concurrency caps; use durable permission checks; rebuild versioned keys gradually | No job loss or scope relaxation; stale epoch keys never served; load remains within safe limits |
| RB-13 Deployment rollback / DevOps | Compare immutable manifest, health checks, schema compatibility and worker lease generation | Pause rollout; deploy previous compatible digest; restore previous publication if safe; use forward repair for irreversible migration; fence old/new workers | Synthetic login/query/report270, preserved control writes and correct tenant isolation; record actual rollback duration |
| RB-14 Customer offboarding / Customer success | Verify authorized request, tenant scope, approved holds and export policy | Pause schedules/extraction; revoke WIF and SaaS access; export authorized retained records; ordered deletion across stores/object versions; retain deletion tombstone | No new collection or download; explicit residual backup expiry/hold; restore test cannot resurrect tenant |
| RB-15 Noisy tenant/cost spike / SRE and FinOps | Inspect per-lane queue wait, active leases, query tags, scan bytes, file counts and actual provider costs | Lower affected admission quota, cancel bounded jobs, preserve interactive capacity; inspect legitimate backfill before throttling permanently | Other tenants recover to measured SLO; accepted coverage preserved; cost estimate reconciled when bill arrives |

## Safe diagnostic command shapes

The implementation supplies a read-only `bridge-admin` CLI with explicit environment and scope, bound to short-lived operator identity. FND-005/OPS-010 must test these interfaces before documentation claims they exist.

```bash
aws sts get-caller-identity --profile bridge-staging-readonly
bridge-admin health account --env staging --tenant TENANT_UUID --account ACCOUNT_UUID
bridge-admin batch inspect --env staging --batch BATCH_UUID
bridge-admin coverage diff --env staging --tenant TENANT_UUID --source QUERY_HISTORY
bridge-admin reconciliation explain --env staging --run RECONCILIATION_UUID
```

Recovery commands require a scoped dry-run manifest containing affected partitions, expected counts/cost deltas, lease generation and rollback reference. An operator reviews this concrete manifest before executing a destructive or production-sensitive action. Reversible retries already authorized by an incident procedure do not need repeated approval.

## Incident and support protocol

SEV1: suspected disclosure, financial corruption broadly served, or total production outage. Page incident commander immediately; initial acknowledgment target15min during contracted coverage. SEV2: major tenant blocked or large coverage backlog; target1h acknowledgment during coverage. SEV3: limited defect with workaround; next-business-day target. These are internal starting targets until staffing and customer contract approve actual commitments. Record impact and update cadence; never promise24×7 response without a staffed rota.

Support intake asks for tenant/request ID, UTC time, page/action, expected behavior and safe screenshot; never asks for passwords, key pairs or unrestricted raw SQL. Customer-facing updates state known impact, workaround and next update time, without speculative cause. Every incident closes with verified recovery, owner, evidence and follow-up tasks.
