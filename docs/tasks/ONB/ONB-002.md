# ONB-002 — Prepare customer and support documentation with rehearsal

## Hierarchy and execution metadata

Domain: ONB — Customer onboarding and first-value acceptance → Epic: E-ONB-01 → Feature: F-ONB-002 → User story: US-ONB-002 → Implementation task: ONB-002 → numbered micro-tasks below → validation → Definition of Done.

Milestone: M9. Owner persona: **Customer success / Snowflake / FinOps**. Implementation status: **NOT_STARTED**. Intended size: one focused implementation session; split into linked child tasks before coding if independent migrations/interfaces cannot fit safely.

## Objective / user story

As the responsible customer success / snowflake / finops, enable independent installation, operation and safe offboarding.

## Why this exists

This delivers **prepare customer and support documentation with rehearsal** as a prerequisite-controlled capability. Its correctness is defined by the explicit oracle below, not by successful compilation. The [canonical contract](../../../docs/18-customer-onboarding/onboarding.md) owns shared definitions; do not invent parallel formulas or schemas.

## Dependencies

- [ONB-001](../ONB/ONB-001.md)
- [OPS-010](../OPS/OPS-010.md)

Dependencies require accepted implementation evidence. A published prerequisite specification is insufficient.

## Inputs

Versioned synthetic fixtures and the accepted outputs of the explicit prerequisites below.

## Outputs and likely files/components

`docs/customer/getting-started.md`, `docs/customer/admin-guide.md`, `docs/customer/offboarding.md`. Add a task-specific fixture and validation target under `tests/spec/ONB-002/`, plus redacted evidence under `docs/evidence/ONB-002/<commit>/`. These are implementation destinations, not files already implemented by this specification.

## Detailed implementation steps

### Micro-task 1

Write administrator installation/revoke steps, FinOps interpretation guide, permissions, billing maturity and history limitations.

### Micro-task 2

Run a clean-room installation with a second engineer using only the guides and record each unclear step.

### Micro-task 3

Test pause/reconnect/offboard guide and explain deletion/backup expiry and report revocation limits.

### Micro-task 4 — Prove and checkpoint

Run the validation below, record expected versus observed results and environment versions, update operational documentation, then checkpoint the task index with the exact next action. Do not mark DONE while a required live gate is unexecuted.

## Data model

Guide version, supported stack/capabilities and verification checklist.

## API / interfaces

Documentation examples use placeholders and refer to canonical contracts; support intake uses request IDs.

## Security and tenant isolation

Customer authorization and selected accounts govern collection. Invitations and ownership assignments are tenant scoped; setup scripts contain identities and grants but no persistent Snowflake secrets.

For this task, test a foreign tenant identifier and an invalid or revoked scope at every input boundary it introduces. Internal infrastructure tasks instead prove environment/principal isolation and absence of public access. Test artifacts use synthetic tenant A/B data only.

## Idempotency / retries

Every step is keyed by tenant/configuration revision; repeated starts reuse active work and accepted batches. Reconnection preserves durable account IDs and data lineage.

## Failure scenarios

Stale screenshot; wrong role name; undocumented network prerequisite; missing deletion caveat.

## Observability

Emit task/component, request/run/batch identifier, outcome, duration and a safe error class. Correlate tenant identifiers in protected logs, not unbounded public metric labels. Exclude credentials, full SQL and raw customer payloads. Record counts/checksums or configuration versions appropriate to this task; alert on repeated failures through the shared platform monitoring contract.

## UX behavior

This task introduces no direct customer page. Its outcome is surfaced through the linked domain capability, health or authorization contract. Emit safe progress/failure metadata; do not expose worker internals or silently render unavailable data as zero. Customer-facing follow-on tasks define their complete interaction states.

## Testing and validation

**Deterministic oracle:** Second engineer reaches synthetic first data without hidden steps; scripts contain no credentials; revoke test prevents new extraction.

**Local:** the FND validation dispatcher must expose `make validate-task TASK=ONB-002 ENV=local`. Implement the fixture before the behavior. Exercise the stated failure cases and retry the identical input; compare outputs with the oracle. Use unit/contract tests and local PostgreSQL/Redis integration where relevant. UI tasks add Playwright state assertions and keyboard checks; data tasks add dbt/Arrow contract assertions.

**AWS / Snowflake:** `make validate-task TASK=ONB-002 ENV=staging` runs only against the isolated staging environment with short-lived identities. Run rehearsal with a separate synthetic customer organization, then collect real customer evidence only after authorization; verify actual grants, history bounds, reconciliation and scoped UX.

**Required test selection:** include the relevant unit, contract, integration, tenant-isolation, security, failure/recovery, E2E/UX, performance and financial levels. Record `NOT_APPLICABLE` with a concrete reason for any level that does not exercise this task. Never replace a specified live behavior with a mock and report it as passed.

## Acceptance criteria

- [ ] Every micro-task output exists at the named destination and agrees with the canonical contract.
- [ ] The deterministic oracle above passes with recorded expected/actual output.
- [ ] Stated failure and replay/retry cases preserve the last accepted state.
- [ ] Unauthorized/environment-crossing cases are denied and audited without leakage.
- [ ] UX and diagnostics represent unknown, partial and failed outcomes accurately.

## Definition of Done

All acceptance criteria plus the [global Definition of Done](../../../DELIVERY_METHODOLOGY.md) pass. Attach the implementation commit, versions, local and required live evidence, reviewer and rollback result. Update the machine task index; specification authoring alone leaves status NOT_STARTED.

## Operational runbook and rollback impact

Add diagnosis, safe retry, rollback and escalation instructions for the failures listed in this task. Record the last safe durable state; never delete evidence to clear a failure.

## Documentation impact

Update the [canonical domain contract](../../../docs/18-customer-onboarding/onboarding.md), affected API/schema examples and task evidence index. Record any changed external assumption in the research register and any architecture change through the ADR process. Preserve one canonical definition rather than copying shared rules into route handlers or other task files.
