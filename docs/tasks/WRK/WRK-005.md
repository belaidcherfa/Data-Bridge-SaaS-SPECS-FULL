# WRK-005 — Implement workload comparison and on-demand deep evidence

## Hierarchy and execution metadata

Domain: WRK — Workload intelligence and execution hierarchies → Epic: E-WRK-01 → Feature: F-WRK-005 → User story: US-WRK-005 → Implementation task: WRK-005 → numbered micro-tasks below → validation → Definition of Done.

Milestone: M5. Owner persona: **Data engineer / Frontend**. Implementation status: **NOT_STARTED**. Intended size: one focused implementation session; split into linked child tasks before coding if independent migrations/interfaces cannot fit safely.

## Objective / user story

As the responsible data engineer / frontend, separate volume, unit-cost and performance regressions with bounded evidence.

## Why this exists

This delivers **implement workload comparison and on-demand deep evidence** as a prerequisite-controlled capability. Its correctness is defined by the explicit oracle below, not by successful compilation. The [canonical contract](../../../docs/10-frontend/workloads.md) owns shared definitions; do not invent parallel formulas or schemas.

## Dependencies

- [WRK-002](../WRK/WRK-002.md)
- [WRK-003](../WRK/WRK-003.md)
- [WRK-004](../WRK/WRK-004.md)
- [API-004](../API/API-004.md)

Dependencies require accepted implementation evidence. A published prerequisite specification is insufficient.

## Inputs

Versioned synthetic fixtures and the accepted outputs of the explicit prerequisites below.

## Outputs and likely files/components

`services/intelligence/workload_compare`, `apps/web/workloads/compare`. Add a task-specific fixture and validation target under `tests/spec/WRK-005/`, plus redacted evidence under `docs/evidence/WRK-005/<commit>/`. These are implementation destinations, not files already implemented by this specification.

## Detailed implementation steps

### Micro-task 1

Require comparable complete windows or label partial; decomposechange by execution volume and unit cost with explicit residual.

### Micro-task 2

Fetch supported operator statistics on demand with strict query ID/account authorization and timeout; sanitizeandstorelimitedevidence.

### Micro-task 3

Test baseline10runs×2=20,current20×3=60; explain+40 as volume+20 and unit cost+20 under fixed decomposition order, without claiming causality.

### Micro-task 4 — Prove and checkpoint

Run the validation below, record expected versus observed results and environment versions, update operational documentation, then checkpoint the task index with the exact next action. Do not mark DONE while a required live gate is unexecuted.

## Data model

A/B scope windows, common/new/disappeared entities, contribution delta and evidence availability.

## API / interfaces

POST /v1/analysis-jobs workload_compare; operator evidence request subject to retention/privilege.

## Security and tenant isolation

Workload tags/comments are untrusted data; parse allowlisted structures and sanitize before central storage. Every hierarchy join carries tenant/account and current access scope.

For this task, test a foreign tenant identifier and an invalid or revoked scope at every input boundary it introduces. Internal infrastructure tasks instead prove environment/principal isolation and absence of public access. Test artifacts use synthetic tenant A/B data only.

## Idempotency / retries

Classifier output is keyed by query+parser version+input revision; reruns replace selected version. Hierarchy aggregates use distinct charge/query links to avoid double attribution.

## Failure scenarios

Unequal windows; insufficient baseline; statistical selection bias; expired query plan; operator permission denied.

## Observability

Emit task/component, request/run/batch identifier, outcome, duration and a safe error class. Correlate tenant identifiers in protected logs, not unbounded public metric labels. Exclude credentials, full SQL and raw customer payloads. Record counts/checksums or configuration versions appropriate to this task; alert on repeated failures through the shared platform monitoring contract.

## UX behavior

| UX requirement | Behavior |
|---|---|
| Persona | Support engineer / workload owner |
| Goal | separate volume, unit-cost and performance regressions with bounded evidence. |
| Entry point | Compare from workload detail |
| Happy path | Choose A/B→review coverage→inspect contributors→request deep evidence. |
| Empty state | Explain minimum baseline requirements. |
| Loading state | Preserve selected scope and filters; show labelled skeletons and a cancellable progress indicator for asynchronous work. |
| Partial-data state | Excludeunmatchedcoverageorlabelresultaspartial. |
| Error state | Keep user input; show a safe error code, retry action and support request ID. Never print SQL, tokens or internal hostnames. |
| Permission-denied state | Return a scoped 403 or non-enumerating 404; show an access-request explanation, with no foreign names, counts or totals. |
| Success state | Show the accepted version, UTC as-of time and completed outcome. Financial results include maturity, coverage and reconciliation status. |
| Drilldown behavior | Changed project/model/activity→hash→query/operator evidence. |
| Primary actions | Compare, investigate, export report, create action. |
| Acceptance criteria | Contributors sum to+40; missing history prevents deleted project claim; expired operator stats shows unavailable without changing cost. |

## Testing and validation

**Deterministic oracle:** Contributors sum to+40; missing history prevents deleted project claim; expired operator stats shows unavailable without changing cost.

**Local:** the FND validation dispatcher must expose `make validate-task TASK=WRK-005 ENV=local`. Implement the fixture before the behavior. Exercise the stated failure cases and retry the identical input; compare outputs with the oracle. Use unit/contract tests and local PostgreSQL/Redis integration where relevant. UI tasks add Playwright state assertions and keyboard checks; data tasks add dbt/Arrow contract assertions.

**AWS / Snowflake:** `make validate-task TASK=WRK-005 ENV=staging` runs only against the isolated staging environment with short-lived identities. Run classification fixtures and live scoped source joins; verify optional native fields against actual schema and prove identical names/IDs in another tenant never link.

**Required test selection:** include the relevant unit, contract, integration, tenant-isolation, security, failure/recovery, E2E/UX, performance and financial levels. Record `NOT_APPLICABLE` with a concrete reason for any level that does not exercise this task. Never replace a specified live behavior with a mock and report it as passed.

## Acceptance criteria

- [ ] Every micro-task output exists at the named destination and agrees with the canonical contract.
- [ ] The deterministic oracle above passes with recorded expected/actual output.
- [ ] Stated failure and replay/retry cases preserve the last accepted state.
- [ ] Unauthorized/environment-crossing cases are denied and audited without leakage.
- [ ] UX and diagnostics represent unknown, partial and failed outcomes accurately.

## Definition of Done

All acceptance criteria plus the [global Definition of Done](../../../DELIVERY_METHODOLOGY.md) pass. Attach the implementation commit, versions, local and required live evidence, reviewer and rollback result. Update the machine task index; specification authoring alone leaves status NOT_STARTED.

## Operational runbook and rollback impact

Add diagnosis, safe retry, rollback and escalation instructions for the failures listed in this task. Record the last safe durable state; never delete evidence to clear a failure.

## Documentation impact

Update the [canonical domain contract](../../../docs/10-frontend/workloads.md), affected API/schema examples and task evidence index. Record any changed external assumption in the research register and any architecture change through the ADR process. Preserve one canonical definition rather than copying shared rules into route handlers or other task files.
