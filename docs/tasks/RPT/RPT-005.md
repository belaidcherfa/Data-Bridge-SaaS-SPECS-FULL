# RPT-005 — Implement report history, secure access and retention

## Hierarchy and execution metadata

Domain: RPT — Reporting, rendering and secure scheduled distribution → Epic: E-RPT-01 → Feature: F-RPT-005 → User story: US-RPT-005 → Implementation task: RPT-005 → numbered micro-tasks below → validation → Definition of Done.

Milestone: M7. Owner persona: **Backend / Frontend / QA**. Implementation status: **NOT_STARTED**. Intended size: one focused implementation session; split into linked child tasks before coding if independent migrations/interfaces cannot fit safely.

## Objective / user story

As the responsible backend / frontend / qa, make report access auditable and revocable.

## Why this exists

This delivers **implement report history, secure access and retention** as a prerequisite-controlled capability. Its correctness is defined by the explicit oracle below, not by successful compilation. The [canonical contract](../../../docs/14-reporting/reporting.md) owns shared definitions; do not invent parallel formulas or schemas.

## Dependencies

- [RPT-004](../RPT/RPT-004.md)
- [SEC-008](../SEC/SEC-008.md)
- [CTL-007](../CTL/CTL-007.md)

Dependencies require accepted implementation evidence. A published prerequisite specification is insufficient.

## Inputs

Versioned synthetic fixtures and the accepted outputs of the explicit prerequisites below.

## Outputs and likely files/components

`apps/web/reports/history`, `apps/api/report-downloads`, `services/reports/retention.py`. Add a task-specific fixture and validation target under `tests/spec/RPT-005/`, plus redacted evidence under `docs/evidence/RPT-005/<commit>/`. These are implementation destinations, not files already implemented by this specification.

## Detailed implementation steps

### Micro-task 1

Build report history with pending/running/ready/failed/expired states and safe retry.

### Micro-task 2

Enforce default30-day artifact retention except approved statement preservation policy; remove object versions according to deletion policy.

### Micro-task 3

Revoke a user after generation and before delivery/download; prove denial and purge abandoned artifacts.

### Micro-task 4 — Prove and checkpoint

Run the validation below, record expected versus observed results and environment versions, update operational documentation, then checkpoint the task index with the exact next action. Do not mark DONE while a required live gate is unexecuted.

## Data model

Artifact retention/expiry and download audit; recipient permission decision recorded without storing report facts in PostgreSQL.

## API / interfaces

Brokered download validates tenant, owner policy, recipient scope and current artifact status.

## Security and tenant isolation

Report ownership, recipients, jobs and object keys are tenant scoped. Rendering is sandboxed, scoped data is handed in explicitly, and every download rechecks current permissions.

For this task, test a foreign tenant identifier and an invalid or revoked scope at every input boundary it introduces. Internal infrastructure tasks instead prove environment/principal isolation and absence of public access. Test artifacts use synthetic tenant A/B data only.

## Idempotency / retries

Definition revisions and occurrence keys deduplicate jobs; files are immutable by job/version; retry renders to a new attempt key then atomically accepts one manifest.

## Failure scenarios

Long-lived presigned URL; expired snapshot; stale permissions; object version retained unexpectedly.

## Observability

Emit task/component, request/run/batch identifier, outcome, duration and a safe error class. Correlate tenant identifiers in protected logs, not unbounded public metric labels. Exclude credentials, full SQL and raw customer payloads. Record counts/checksums or configuration versions appropriate to this task; alert on repeated failures through the shared platform monitoring contract.

## UX behavior

| UX requirement | Behavior |
|---|---|
| Persona | CFO / FinOps / team lead |
| Goal | make report access auditable and revocable. |
| Entry point | /reports |
| Happy path | Find run → inspect period/status → open or download. |
| Empty state | Create or schedule a first report. |
| Loading state | Preserve selected scope and filters; show labelled skeletons and a cancellable progress indicator for asynchronous work. |
| Partial-data state | Show maturity and source coverage before download. |
| Error state | Keep user input; show a safe error code, retry action and support request ID. Never print SQL, tokens or internal hostnames. |
| Permission-denied state | Return a scoped 403 or non-enumerating 404; show an access-request explanation, with no foreign names, counts or totals. |
| Success state | Show the accepted version, UTC as-of time and completed outcome. Financial results include maturity, coverage and reconciliation status. |
| Drilldown behavior | Run → source publication and delivery outcomes. |
| Primary actions | Download, regenerate, cancel, inspect receipt, delete allowed artifact. |
| Acceptance criteria | Revoked user receives no artifact; guessed foreign job ID reveals nothing; expired link offers authorized regeneration; retention does not erase required closed-statement evidence. |

## Testing and validation

**Deterministic oracle:** Revoked user receives no artifact; guessed foreign job ID reveals nothing; expired link offers authorized regeneration; retention does not erase required closed-statement evidence.

**Local:** the FND validation dispatcher must expose `make validate-task TASK=RPT-005 ENV=local`. Implement the fixture before the behavior. Exercise the stated failure cases and retry the identical input; compare outputs with the oracle. Use unit/contract tests and local PostgreSQL/Redis integration where relevant. UI tasks add Playwright state assertions and keyboard checks; data tasks add dbt/Arrow contract assertions.

**AWS / Snowflake:** `make validate-task TASK=RPT-005 ENV=staging` runs only against the isolated staging environment with short-lived identities. Use staging S3/KMS and isolated browser workers; verify revoked recipients, malicious labels/URLs, duplicate schedule ticks, output checksums and visual/numerical parity.

**Required test selection:** include the relevant unit, contract, integration, tenant-isolation, security, failure/recovery, E2E/UX, performance and financial levels. Record `NOT_APPLICABLE` with a concrete reason for any level that does not exercise this task. Never replace a specified live behavior with a mock and report it as passed.

## Acceptance criteria

- [ ] Every micro-task output exists at the named destination and agrees with the canonical contract.
- [ ] The deterministic oracle above passes with recorded expected/actual output.
- [ ] Stated failure and replay/retry cases preserve the last accepted state.
- [ ] Unauthorized/environment-crossing cases are denied and audited without leakage.
- [ ] UX and diagnostics represent unknown, partial and failed outcomes accurately.

## Definition of Done

All acceptance criteria plus the [global Definition of Done](../../../DELIVERY_METHODOLOGY.md) pass. Attach the implementation commit, versions, local and required live evidence, reviewer and rollback result. Update the machine task index; specification authoring alone leaves status NOT_STARTED.

## Operational runbook and rollback impact

Add diagnosis, safe retry, rollback and escalation instructions for the failures listed in this task. Record the last safe durable state; never delete evidence to clear a failure.

## Documentation impact

Update the [canonical domain contract](../../../docs/14-reporting/reporting.md), affected API/schema examples and task evidence index. Record any changed external assumption in the research register and any architecture change through the ADR process. Preserve one canonical definition rather than copying shared rules into route handlers or other task files.
