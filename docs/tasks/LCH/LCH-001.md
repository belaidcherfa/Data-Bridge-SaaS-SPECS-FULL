# LCH-001 — Define plans, entitlements and manual billing workflow

## Hierarchy and execution metadata

Domain: LCH — Commercial activation, first release and post-launch validation → Epic: E-LCH-01 → Feature: F-LCH-001 → User story: US-LCH-001 → Implementation task: LCH-001 → numbered micro-tasks below → validation → Definition of Done.

Milestone: M10. Owner persona: **Product / Delivery / Customer success**. Implementation status: **NOT_STARTED**. Intended size: one focused implementation session; split into linked child tasks before coding if independent migrations/interfaces cannot fit safely.

## Objective / user story

As the responsible product / delivery / customer success, activate a first customer with an auditable commercial basis.

## Why this exists

This delivers **define plans, entitlements and manual billing workflow** as a prerequisite-controlled capability. Its correctness is defined by the explicit oracle below, not by successful compilation. The [canonical contract](../../../docs/20-launch/launch.md) owns shared definitions; do not invent parallel formulas or schemas.

## Dependencies

- [REL-003](../REL/REL-003.md)
- [CTL-007](../CTL/CTL-007.md)

Dependencies require accepted implementation evidence. A published prerequisite specification is insufficient.

## Inputs

Versioned synthetic fixtures and the accepted outputs of the explicit prerequisites below.

## Outputs and likely files/components

`apps/api/entitlements`, `apps/web/settings/billing`, `docs/commercial`. Add a task-specific fixture and validation target under `tests/spec/LCH-001/`, plus redacted evidence under `docs/evidence/LCH-001/<commit>/`. These are implementation destinations, not files already implemented by this specification.

## Detailed implementation steps

### Micro-task 1

Define initial approved plan and grace/export policies; show limits before costly backfill admission.

### Micro-task 2

Implement audited manual invoice/payment evidence entry with duplicate reference guards and correction review.

### Micro-task 3

Test trial, pending, paid, past-due and cancelled states; prove read and job permissions follow explicit policy.

### Micro-task 4 — Prove and checkpoint

Run the validation below, record expected versus observed results and environment versions, update operational documentation, then checkpoint the task index with the exact next action. Do not mark DONE while a required live gate is unexecuted.

## Data model

Plan limits/features/effective revision, contract/invoice/payment references and subscription state; no card or analytical fact storage.

## API / interfaces

Authorized plan/entitlement commands; admission reads effective limits with scoped cache invalidation.

## Security and tenant isolation

Commercial mutation permissions are separate from analytical read scopes. Customer identities, contracts and payment evidence stay in approved private systems; repository templates are synthetic.

For this task, test a foreign tenant identifier and an invalid or revoked scope at every input boundary it introduces. Internal infrastructure tasks instead prove environment/principal isolation and absence of public access. Test artifacts use synthetic tenant A/B data only.

## Idempotency / retries

Payment/invoice references and entitlement commands use uniqueness plus expected revision; release promotion is immutable; recurring review records key by customer/window.

## Failure scenarios

False paid marker; plan race; expired cache; duplicate invoice; cancellation erases evidence.

## Observability

Emit task/component, request/run/batch identifier, outcome, duration and a safe error class. Correlate tenant identifiers in protected logs, not unbounded public metric labels. Exclude credentials, full SQL and raw customer payloads. Record counts/checksums or configuration versions appropriate to this task; alert on repeated failures through the shared platform monitoring contract.

## UX behavior

| UX requirement | Behavior |
|---|---|
| Persona | Customer administrator / FinOps owner |
| Goal | activate a first customer with an auditable commercial basis. |
| Entry point | /settings/billing |
| Happy path | Review plan → inspect invoice status → contact billing owner. |
| Empty state | Show trial terms and required commercial steps. |
| Loading state | Preserve selected scope and filters; show labelled skeletons and a cancellable progress indicator for asynchronous work. |
| Partial-data state | Distinguish pending verification from settled payment. |
| Error state | Keep user input; show a safe error code, retry action and support request ID. Never print SQL, tokens or internal hostnames. |
| Permission-denied state | Return a scoped 403 or non-enumerating 404; show an access-request explanation, with no foreign names, counts or totals. |
| Success state | Show the accepted version, UTC as-of time and completed outcome. Financial results include maturity, coverage and reconciliation status. |
| Drilldown behavior | Entitlement → policy/effective date and permitted invoice reference. |
| Primary actions | Review plan, request change, contact billing, export authorized data. |
| Acceptance criteria | Duplicate payment reference creates one activation; pending invoice is not paid; quota breach rejects new job without deleting accepted history. |

## Testing and validation

**Deterministic oracle:** Duplicate payment reference creates one activation; pending invoice is not paid; quota breach rejects new job without deleting accepted history.

**Local:** the FND validation dispatcher must expose `make validate-task TASK=LCH-001 ENV=local`. Implement the fixture before the behavior. Exercise the stated failure cases and retry the identical input; compare outputs with the oracle. Use unit/contract tests and local PostgreSQL/Redis integration where relevant. UI tasks add Playwright state assertions and keyboard checks; data tasks add dbt/Arrow contract assertions.

**AWS / Snowflake:** `make validate-task TASK=LCH-001 ENV=staging` runs only against the isolated staging environment with short-lived identities. Validate entitlement gates and release smoke tests in staging before authorized production activation; actual payment and customer acceptance require genuine recorded evidence.

**Required test selection:** include the relevant unit, contract, integration, tenant-isolation, security, failure/recovery, E2E/UX, performance and financial levels. Record `NOT_APPLICABLE` with a concrete reason for any level that does not exercise this task. Never replace a specified live behavior with a mock and report it as passed.

## Acceptance criteria

- [ ] Every micro-task output exists at the named destination and agrees with the canonical contract.
- [ ] The deterministic oracle above passes with recorded expected/actual output.
- [ ] Stated failure and replay/retry cases preserve the last accepted state.
- [ ] Unauthorized/environment-crossing cases are denied and audited without leakage.
- [ ] UX and diagnostics represent unknown, partial and failed outcomes accurately.

## Definition of Done

All acceptance criteria plus the [global Definition of Done](../../../DELIVERY_METHODOLOGY.md) pass. Attach the implementation commit, versions, local and required live evidence, reviewer and rollback result. Update the machine task index; specification authoring alone leaves status NOT_STARTED.

## Operational runbook and rollback impact

Add diagnosis, safe retry, rollback and escalation instructions for the failures listed in this task. Record the last safe durable state; never delete evidence to clear a failure.

## Documentation impact

Update the [canonical domain contract](../../../docs/20-launch/launch.md), affected API/schema examples and task evidence index. Record any changed external assumption in the research register and any architecture change through the ADR process. Preserve one canonical definition rather than copying shared rules into route handlers or other task files.
