# INF-001 — Bootstrap AWS accounts, state and environment guards

## Hierarchy and execution metadata

Domain: INF — AWS platform, networking and deployment foundation → Epic: E-INF-01 → Feature: F-INF-001 → User story: US-INF-001 → Implementation task: INF-001 → numbered micro-tasks below → validation → Definition of Done.

Milestone: M1. Owner persona: **DevOps**. Implementation status: **NOT_STARTED**. Intended size: one focused implementation session; split into linked child tasks before coding if independent migrations/interfaces cannot fit safely.

## Objective / user story

As the responsible devops, provision infrastructure without accidental cross-environment changes.

## Why this exists

This delivers **bootstrap aws accounts, state and environment guards** as a prerequisite-controlled capability. Its correctness is defined by the explicit oracle below, not by successful compilation. The [canonical contract](../../../docs/17-devops/platform.md) owns shared definitions; do not invent parallel formulas or schemas.

## Dependencies

- [FND-006](../FND/FND-006.md)

Dependencies require accepted implementation evidence. A published prerequisite specification is insufficient.

## Inputs

Versioned synthetic fixtures and the accepted outputs of the explicit prerequisites below.

## Outputs and likely files/components

`infra/terraform/bootstrap`, `infra/terraform/environments`, `docs/runbooks/terraform.md`. Add a task-specific fixture and validation target under `tests/spec/INF-001/`, plus redacted evidence under `docs/evidence/INF-001/<commit>/`. These are implementation destinations, not files already implemented by this specification.

## Detailed implementation steps

### Micro-task 1

Document human bootstrap permissions, organizational account creation and billing owners; leave actual account identifiers as required deployment inputs.

### Micro-task 2

Create encrypted versioned state buckets and locking configuration supported by the pinned Terraform version; import bootstrap resources into managed state.

### Micro-task 3

Run two simultaneous plan/apply attempts in staging and prove locking; deliberately target the wrong account and reject before a resource mutation.

### Micro-task 4 — Prove and checkpoint

Run the validation below, record expected versus observed results and environment versions, update operational documentation, then checkpoint the task index with the exact next action. Do not mark DONE while a required live gate is unexecuted.

## Data model

Environment manifest: AWS account ID, region, Snowflake account, DNS zone, state bucket/KMS ARN; no fabricated IDs.

## API / interfaces

terraform init/plan/apply with expected_account_id and region assertions.

## Security and tenant isolation

IAM roles are environment-specific. No broad iam:PassRole, wildcard data access or public database/cache/Dagster. Verify access with both allowed and denied principals.

For this task, test a foreign tenant identifier and an invalid or revoked scope at every input boundary it introduces. Internal infrastructure tasks instead prove environment/principal isolation and absence of public access. Test artifacts use synthetic tenant A/B data only.

## Idempotency / retries

Terraform plans are repeatable; apply exact reviewed plan with state locking. ECS retries use immutable image/task revisions; queue jobs use durable idempotency keys.

## Failure scenarios

Lost state lock; wrong region; state secret exposure; partial bootstrap; missing organizational authority.

## Observability

Emit task/component, request/run/batch identifier, outcome, duration and a safe error class. Correlate tenant identifiers in protected logs, not unbounded public metric labels. Exclude credentials, full SQL and raw customer payloads. Record counts/checksums or configuration versions appropriate to this task; alert on repeated failures through the shared platform monitoring contract.

## UX behavior

This task introduces no direct customer page. Its outcome is surfaced through the linked domain capability, health or authorization contract. Emit safe progress/failure metadata; do not expose worker internals or silently render unavailable data as zero. Customer-facing follow-on tasks define their complete interaction states.

## Testing and validation

**Deterministic oracle:** Second apply has no changes; wrong AWS account aborts; concurrent mutation is blocked; state recovery from a prior version is documented.

**Local:** the FND validation dispatcher must expose `make validate-task TASK=INF-001 ENV=local`. Implement the fixture before the behavior. Exercise the stated failure cases and retry the identical input; compare outputs with the oracle. Use unit/contract tests and local PostgreSQL/Redis integration where relevant. UI tasks add Playwright state assertions and keyboard checks; data tasks add dbt/Arrow contract assertions.

**AWS / Snowflake:** `make validate-task TASK=INF-001 ENV=staging` runs only against the isolated staging environment with short-lived identities. Run Terraform plan/apply in STAGING only, inspect actual routes/security groups/IAM with positive and negative probes, then prove failover and rollback using synthetic traffic.

**Required test selection:** include the relevant unit, contract, integration, tenant-isolation, security, failure/recovery, E2E/UX, performance and financial levels. Record `NOT_APPLICABLE` with a concrete reason for any level that does not exercise this task. Never replace a specified live behavior with a mock and report it as passed.

## Acceptance criteria

- [ ] Every micro-task output exists at the named destination and agrees with the canonical contract.
- [ ] The deterministic oracle above passes with recorded expected/actual output.
- [ ] Stated failure and replay/retry cases preserve the last accepted state.
- [ ] Unauthorized/environment-crossing cases are denied and audited without leakage.
- [ ] UX and diagnostics represent unknown, partial and failed outcomes accurately.

## Definition of Done

All acceptance criteria plus the [global Definition of Done](../../../DELIVERY_METHODOLOGY.md) pass. Attach the implementation commit, versions, local and required live evidence, reviewer and rollback result. Update the machine task index; specification authoring alone leaves status NOT_STARTED.

## Operational runbook and rollback impact

Add diagnosis, safe retry, rollback and escalation instructions for the failures listed in this task. Record the last safe durable state; never delete evidence to clear a failure.

## Documentation impact

Update the [canonical domain contract](../../../docs/17-devops/platform.md), affected API/schema examples and task evidence index. Record any changed external assumption in the research register and any architecture change through the ADR process. Preserve one canonical definition rather than copying shared rules into route handlers or other task files.
