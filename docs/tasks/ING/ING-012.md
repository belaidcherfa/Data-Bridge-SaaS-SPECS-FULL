# ING-012 — Add bounded hot history and truthful Data Health UX

## Hierarchy and execution metadata

Domain: ING — Source contracts and durable immutable ingestion → Epic: E-ING-01 → Feature: F-ING-012 → User story: US-ING-012 → Implementation task: ING-012 → numbered micro-tasks below → validation → Definition of Done.

Milestone: M3. Owner persona: **Data engineer**. Implementation status: **NOT_STARTED**. Intended size: one focused implementation session; split into linked child tasks before coding if independent migrations/interfaces cannot fit safely.

## Objective / user story

As the responsible data engineer, show recent activity and actual synchronization coverage clearly.

## Why this exists

This delivers **add bounded hot history and truthful data health ux** as a prerequisite-controlled capability. Its correctness is defined by the explicit oracle below, not by successful compilation. The [canonical contract](../../../docs/05-ingestion/ingestion.md) owns shared definitions; do not invent parallel formulas or schemas.

## Dependencies

- [ING-011](../ING/ING-011.md)

Dependencies require accepted implementation evidence. A published prerequisite specification is insufficient.

## Inputs

Versioned synthetic fixtures and the accepted outputs of the explicit prerequisites below.

## Outputs and likely files/components

`services/extractor/hot.py`, `apps/web/data_health`, `apps/api/data_health`. Add a task-specific fixture and validation target under `tests/spec/ING-012/`, plus redacted evidence under `docs/evidence/ING-012/<commit>/`. These are implementation destinations, not files already implemented by this specification.

## Detailed implementation steps

### Micro-task 1

Implement optional recent history with documented function limits; bisect saturated result windows and mark irreducible overflow as incomplete, never silently complete.

### Micro-task 2

Merge hot and authoritative histories by key/priority; query costs remain unknown unless a separately documented estimate is enabled.

### Micro-task 3

Render account/source progress, capability gaps, batch diagnostics and retry actions without exposing Dagster; validate empty vs delayed vs failing states.

### Micro-task 4 — Prove and checkpoint

Run the validation below, record expected versus observed results and environment versions, update operational documentation, then checkpoint the task index with the exact next action. Do not mark DONE while a required live gate is unexecuted.

## Data model

Hot rows keyed by tenant/account/query and maturity; health has source horizon, transport/transform lag, gaps and last error.

## API / interfaces

GET /v1/data-health; optional bounded INFORMATION_SCHEMA history; authority precedence by source contract.

## Security and tenant isolation

Extractors receive one account identity and prefix; sanitize before serialization; reject file/prefix/manifest identity mismatch. RAW and quarantine are not product-readable.

For this task, test a foreign tenant identifier and an invalid or revoked scope at every input boundary it introduces. Internal infrastructure tasks instead prove environment/principal isolation and absence of public access. Test artifacts use synthetic tenant A/B data only.

## Idempotency / retries

Immutable attempts plus stable logical window IDs, fenced leases, acceptance gates, deterministic keys and contiguous checkpoints provide idempotent business results over at-least-once transport.

## Failure scenarios

Near-now truncation; overlapping functions; stale hot copy overrides final; progress percent decreases without explanation.

## Observability

Emit task/component, request/run/batch identifier, outcome, duration and a safe error class. Correlate tenant identifiers in protected logs, not unbounded public metric labels. Exclude credentials, full SQL and raw customer payloads. Record counts/checksums or configuration versions appropriate to this task; alert on repeated failures through the shared platform monitoring contract.

## UX behavior

| UX requirement | Behavior |
|---|---|
| Persona | Snowflake administrator |
| Goal | show recent activity and actual synchronization coverage clearly. |
| Entry point | /platform/data-health |
| Happy path | Select organization/account → inspect source coverage → diagnose stage → retry permitted failure. |
| Empty state | No connection: open connection wizard; no usage: show confirmed empty intervals. |
| Loading state | Preserve selected scope and filters; show labelled skeletons and a cancellable progress indicator for asynchronous work. |
| Partial-data state | Show requested/available/accepted/published ranges and missing-source reasons. |
| Error state | Keep user input; show a safe error code, retry action and support request ID. Never print SQL, tokens or internal hostnames. |
| Permission-denied state | Return a scoped 403 or non-enumerating 404; show an access-request explanation, with no foreign names, counts or totals. |
| Success state | Show the accepted version, UTC as-of time and completed outcome. Financial results include maturity, coverage and reconciliation status. |
| Drilldown behavior | Account → source → window → safe batch/load diagnostics. |
| Primary actions | Start backfill, pause, resume, retry, request replay, export health summary. |
| Acceptance criteria | Hot+authoritative copy of Q1 counts once; capped result set displays incomplete coverage; source latency is shown separately from pipeline lag. |

## Testing and validation

**Deterministic oracle:** Hot+authoritative copy of Q1 counts once; capped result set displays incomplete coverage; source latency is shown separately from pipeline lag.

**Local:** the FND validation dispatcher must expose `make validate-task TASK=ING-012 ENV=local`. Implement the fixture before the behavior. Exercise the stated failure cases and retry the identical input; compare outputs with the oracle. Use unit/contract tests and local PostgreSQL/Redis integration where relevant. UI tasks add Playwright state assertions and keyboard checks; data tasks add dbt/Arrow contract assertions.

**AWS / Snowflake:** `make validate-task TASK=ING-012 ENV=staging` runs only against the isolated staging environment with short-lived identities. Exercise real Fargate→customer Snowflake→S3/Snowpipe→central RAW in staging; inject duplicate events, killed workers, missing files, late corrections and replay while comparing exact row/credit totals.

**Required test selection:** include the relevant unit, contract, integration, tenant-isolation, security, failure/recovery, E2E/UX, performance and financial levels. Record `NOT_APPLICABLE` with a concrete reason for any level that does not exercise this task. Never replace a specified live behavior with a mock and report it as passed.

## Acceptance criteria

- [ ] Every micro-task output exists at the named destination and agrees with the canonical contract.
- [ ] The deterministic oracle above passes with recorded expected/actual output.
- [ ] Stated failure and replay/retry cases preserve the last accepted state.
- [ ] Unauthorized/environment-crossing cases are denied and audited without leakage.
- [ ] UX and diagnostics represent unknown, partial and failed outcomes accurately.

## Definition of Done

All acceptance criteria plus the [global Definition of Done](../../../DELIVERY_METHODOLOGY.md) pass. Attach the implementation commit, versions, local and required live evidence, reviewer and rollback result. Update the machine task index; specification authoring alone leaves status NOT_STARTED.

## Operational runbook and rollback impact

Add diagnosis, safe retry, rollback and escalation instructions for the failures listed in this task. Record the last safe durable state; never delete evidence to clear a failure.

## Documentation impact

Update the [canonical domain contract](../../../docs/05-ingestion/ingestion.md), affected API/schema examples and task evidence index. Record any changed external assumption in the research register and any architecture change through the ADR process. Preserve one canonical definition rather than copying shared rules into route handlers or other task files.
