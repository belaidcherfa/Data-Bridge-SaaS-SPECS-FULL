# SEC-008 — Create audit trail and early isolation attack suite

## Hierarchy and execution metadata

Domain: SEC — Identity, authorization, isolation and privacy → Epic: E-SEC-01 → Feature: F-SEC-008 → User story: US-SEC-008 → Implementation task: SEC-008 → numbered micro-tasks below → validation → Definition of Done.

Milestone: M1. Owner persona: **Security**. Implementation status: **NOT_STARTED**. Intended size: one focused implementation session; split into linked child tasks before coding if independent migrations/interfaces cannot fit safely.

## Objective / user story

As the responsible security, prove traceability and detect tenant leaks before feature development.

## Why this exists

This delivers **create audit trail and early isolation attack suite** as a prerequisite-controlled capability. Its correctness is defined by the explicit oracle below, not by successful compilation. The [canonical contract](../../../docs/02-security/security.md) owns shared definitions; do not invent parallel formulas or schemas.

## Dependencies

- [SEC-006](../SEC/SEC-006.md)
- [SEC-007](../SEC/SEC-007.md)

Dependencies require accepted implementation evidence. A published prerequisite specification is insufficient.

## Inputs

Versioned synthetic fixtures and the accepted outputs of the explicit prerequisites below.

## Outputs and likely files/components

`apps/api/audit`, `services/audit_export`, `tests/security/isolation`. Add a task-specific fixture and validation target under `tests/spec/SEC-008/`, plus redacted evidence under `docs/evidence/SEC-008/<commit>/`. These are implementation destinations, not files already implemented by this specification.

## Detailed implementation steps

### Micro-task 1

Instrument privileged auth, scope, connection, rule, export and billing actions with outcome and version; exclude secrets from diffs.

### Micro-task 2

Export audit batches to a separately controlled bucket; test verification after one record is changed or deleted in a disposable copy.

### Micro-task 3

Create reusable attack fixtures for path IDs, filters, cache, job, cursor, S3 link and SQL scope; run them in feature CI.

### Micro-task 4 — Prove and checkpoint

Run the validation below, record expected versus observed results and environment versions, update operational documentation, then checkpoint the task index with the exact next action. Do not mark DONE while a required live gate is unexecuted.

## Data model

audit.event UUID with redacted before/after and append-only writer; export batch checksum/previous digest.

## API / interfaces

Audit emit within transaction/outbox; tenant-scoped audit query and restricted security export.

## Security and tenant isolation

Authorization derives from verified identity plus current membership; never trust tenant/profile from raw input. Test foreign tenant and narrower same-tenant scope separately.

For this task, test a foreign tenant identifier and an invalid or revoked scope at every input boundary it introduces. Internal infrastructure tasks instead prove environment/principal isolation and absence of public access. Test artifacts use synthetic tenant A/B data only.

## Idempotency / retries

Auth callbacks use one-time state/nonce and PKCE verifier; invitation/revocation writes are transactionally idempotent. Permission epoch is monotonic; audit duplicates deduplicate by event_id.

## Failure scenarios

Audit write failure; export duplicate; truncated hash chain; overly broad auditor scope; swallowed denial event.

## Observability

Emit task/component, request/run/batch identifier, outcome, duration and a safe error class. Correlate tenant identifiers in protected logs, not unbounded public metric labels. Exclude credentials, full SQL and raw customer payloads. Record counts/checksums or configuration versions appropriate to this task; alert on repeated failures through the shared platform monitoring contract.

## UX behavior

This task introduces no direct customer page. Its outcome is surfaced through the linked domain capability, health or authorization contract. Emit safe progress/failure metadata; do not expose worker internals or silently render unavailable data as zero. Customer-facing follow-on tasks define their complete interaction states.

## Testing and validation

**Deterministic oracle:** Foreign IDs never disclose names/counts; tampered audit fixture fails verification; a denied mutation still creates a safe audit event.

**Local:** the FND validation dispatcher must expose `make validate-task TASK=SEC-008 ENV=local`. Implement the fixture before the behavior. Exercise the stated failure cases and retry the identical input; compare outputs with the oracle. Use unit/contract tests and local PostgreSQL/Redis integration where relevant. UI tasks add Playwright state assertions and keyboard checks; data tasks add dbt/Arrow contract assertions.

**AWS / Snowflake:** `make validate-task TASK=SEC-008 ENV=staging` runs only against the isolated staging environment with short-lived identities. Prove Cognito positive/negative token flows, PostgreSQL pooled-context attacks and Snowflake policy enforcement with two live staging principals; mocks are insufficient.

**Required test selection:** include the relevant unit, contract, integration, tenant-isolation, security, failure/recovery, E2E/UX, performance and financial levels. Record `NOT_APPLICABLE` with a concrete reason for any level that does not exercise this task. Never replace a specified live behavior with a mock and report it as passed.

## Acceptance criteria

- [ ] Every micro-task output exists at the named destination and agrees with the canonical contract.
- [ ] The deterministic oracle above passes with recorded expected/actual output.
- [ ] Stated failure and replay/retry cases preserve the last accepted state.
- [ ] Unauthorized/environment-crossing cases are denied and audited without leakage.
- [ ] UX and diagnostics represent unknown, partial and failed outcomes accurately.

## Definition of Done

All acceptance criteria plus the [global Definition of Done](../../../DELIVERY_METHODOLOGY.md) pass. Attach the implementation commit, versions, local and required live evidence, reviewer and rollback result. Update the machine task index; specification authoring alone leaves status NOT_STARTED.

## Operational runbook and rollback impact

Add diagnosis, safe retry, rollback and escalation instructions for the failures listed in this task. Record the last safe durable state; never delete evidence to clear a failure.

## Documentation impact

Update the [canonical domain contract](../../../docs/02-security/security.md), affected API/schema examples and task evidence index. Record any changed external assumption in the research register and any architecture change through the ADR process. Preserve one canonical definition rather than copying shared rules into route handlers or other task files.
