# API-004 — Build asynchronous analysis jobs and cancellation

## Hierarchy and execution metadata

Domain: API — Semantic metric registry and analytical API → Epic: E-API-01 → Feature: F-API-004 → User story: US-API-004 → Implementation task: API-004 → numbered micro-tasks below → validation → Definition of Done.

Milestone: M5. Owner persona: **Backend engineer**. Implementation status: **NOT_STARTED**. Intended size: one focused implementation session; split into linked child tasks before coding if independent migrations/interfaces cannot fit safely.

## Objective / user story

As the responsible backend engineer, support heavy multi-account analyses without blocking HTTP workers.

## Why this exists

This delivers **build asynchronous analysis jobs and cancellation** as a prerequisite-controlled capability. Its correctness is defined by the explicit oracle below, not by successful compilation. The [canonical contract](../../../docs/09-api/semantic-api.md) owns shared definitions; do not invent parallel formulas or schemas.

## Dependencies

- [API-003](../API/API-003.md)
- [CTL-004](../CTL/CTL-004.md)
- [ORC-006](../ORC/ORC-006.md)

Dependencies require accepted implementation evidence. A published prerequisite specification is insufficient.

## Inputs

Versioned synthetic fixtures and the accepted outputs of the explicit prerequisites below.

## Outputs and likely files/components

`apps/api/analysis_jobs`, `services/intelligence/jobs.py`. Add a task-specific fixture and validation target under `tests/spec/API-004/`, plus redacted evidence under `docs/evidence/API-004/<commit>/`. These are implementation destinations, not files already implemented by this specification.

## Detailed implementation steps

### Micro-task 1

Validate cost/cardinality and quotas, create job+outbox atomically and return202 with polling interval.

### Micro-task 2

Run under pinned data/config/authorization context, checkpoint progress and publish result only on complete validation.

### Micro-task 3

Retry submission/worker execution, cancel mid run and revoke requester before download; verify no duplicate result or unauthorized return.

### Micro-task 4 — Prove and checkpoint

Run the validation below, record expected versus observed results and environment versions, update operational documentation, then checkpoint the task index with the exact next action. Do not mark DONE while a required live gate is unexecuted.

## Data model

Control job definition/status/owner/scope/publication; result manifest in Snowflake; no analytical rows in PostgreSQL.

## API / interfaces

POST /v1/analysis-jobs; GET /{id}; POST /{id}/cancel; status QUEUED/RUNNING/SUCCEEDED/FAILED/CANCELLED.

## Security and tenant isolation

Validate current membership, entitlement and query dimensions before cache/query planning. Principal/profile comes from server mapping; cursors/jobs/explain references are tenant- and scope-bound.

For this task, test a foreign tenant identifier and an invalid or revoked scope at every input boundary it introduces. Internal infrastructure tasks instead prove environment/principal isolation and absence of public access. Test artifacts use synthetic tenant A/B data only.

## Idempotency / retries

Reads pin publication; POST job/export writes use request hash+idempotency key and durable outbox. Cursor replay is safe within expiry; cancellation and completion compare fencing/revision.

## Failure scenarios

Queue delay; orphan job; cancellation races completion; result snapshot expired; tenant quota exhausted.

## Observability

Emit task/component, request/run/batch identifier, outcome, duration and a safe error class. Correlate tenant identifiers in protected logs, not unbounded public metric labels. Exclude credentials, full SQL and raw customer payloads. Record counts/checksums or configuration versions appropriate to this task; alert on repeated failures through the shared platform monitoring contract.

## UX behavior

| UX requirement | Behavior |
|---|---|
| Persona | Analyst |
| Goal | support heavy multi-account analyses without blocking HTTP workers. |
| Entry point | Analyze action from explorer |
| Happy path | Submit deep analysis → follow progress → open completed result. |
| Empty state | Explain required scope/period before submission. |
| Loading state | Preserve selected scope and filters; show labelled skeletons and a cancellable progress indicator for asynchronous work. |
| Partial-data state | Show covered partitions and outstanding work without claiming final totals. |
| Error state | Keep user input; show a safe error code, retry action and support request ID. Never print SQL, tokens or internal hostnames. |
| Permission-denied state | Return a scoped 403 or non-enumerating 404; show an access-request explanation, with no foreign names, counts or totals. |
| Success state | Show the accepted version, UTC as-of time and completed outcome. Financial results include maturity, coverage and reconciliation status. |
| Drilldown behavior | Job → reproducible result → source coverage/evidence. |
| Primary actions | Start, cancel, retry, open result, export. |
| Acceptance criteria | Identical request key creates one job; API responds promptly; job result remains scoped; revoked user cannot download finished output. |

## Testing and validation

**Deterministic oracle:** Identical request key creates one job; API responds promptly; job result remains scoped; revoked user cannot download finished output.

**Local:** the FND validation dispatcher must expose `make validate-task TASK=API-004 ENV=local`. Implement the fixture before the behavior. Exercise the stated failure cases and retry the identical input; compare outputs with the oracle. Use unit/contract tests and local PostgreSQL/Redis integration where relevant. UI tasks add Playwright state assertions and keyboard checks; data tasks add dbt/Arrow contract assertions.

**AWS / Snowflake:** `make validate-task TASK=API-004 ENV=staging` runs only against the isolated staging environment with short-lived identities. Run OpenAPI contract tests and real Snowflake query/policy tests under tenant A/B profiles; benchmark cached/cold and high-cardinality requests with cancellation and revocation.

**Required test selection:** include the relevant unit, contract, integration, tenant-isolation, security, failure/recovery, E2E/UX, performance and financial levels. Record `NOT_APPLICABLE` with a concrete reason for any level that does not exercise this task. Never replace a specified live behavior with a mock and report it as passed.

## Acceptance criteria

- [ ] Every micro-task output exists at the named destination and agrees with the canonical contract.
- [ ] The deterministic oracle above passes with recorded expected/actual output.
- [ ] Stated failure and replay/retry cases preserve the last accepted state.
- [ ] Unauthorized/environment-crossing cases are denied and audited without leakage.
- [ ] UX and diagnostics represent unknown, partial and failed outcomes accurately.

## Definition of Done

All acceptance criteria plus the [global Definition of Done](../../../DELIVERY_METHODOLOGY.md) pass. Attach the implementation commit, versions, local and required live evidence, reviewer and rollback result. Update the machine task index; specification authoring alone leaves status NOT_STARTED.

## Operational runbook and rollback impact

Add diagnosis, safe retry, rollback and escalation instructions for the failures listed in this task. Record the last safe durable state; never delete evidence to clear a failure.

## Documentation impact

Update the [canonical domain contract](../../../docs/09-api/semantic-api.md), affected API/schema examples and task evidence index. Record any changed external assumption in the research register and any architecture change through the ADR process. Preserve one canonical definition rather than copying shared rules into route handlers or other task files.
