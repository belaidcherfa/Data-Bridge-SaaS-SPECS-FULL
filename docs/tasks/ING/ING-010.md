# ING-010 — Plan historical backfills and catch-up with fair admission

## Hierarchy and execution metadata

Domain: ING — Source contracts and durable immutable ingestion → Epic: E-ING-01 → Feature: F-ING-010 → User story: US-ING-010 → Implementation task: ING-010 → numbered micro-tasks below → validation → Definition of Done.

Milestone: M3. Owner persona: **Data engineer**. Implementation status: **NOT_STARTED**. Intended size: one focused implementation session; split into linked child tasks before coding if independent migrations/interfaces cannot fit safely.

## Objective / user story

As the responsible data engineer, synchronize requested history without starving existing customers.

## Why this exists

This delivers **plan historical backfills and catch-up with fair admission** as a prerequisite-controlled capability. Its correctness is defined by the explicit oracle below, not by successful compilation. The [canonical contract](../../../docs/05-ingestion/ingestion.md) owns shared definitions; do not invent parallel formulas or schemas.

## Dependencies

- [ING-009](../ING/ING-009.md)
- [CON-006](../CON/CON-006.md)

Dependencies require accepted implementation evidence. A published prerequisite specification is insufficient.

## Inputs

Versioned synthetic fixtures and the accepted outputs of the explicit prerequisites below.

## Outputs and likely files/components

`services/ingestion/backfill.py`, `apps/api/sync`. Add a task-specific fixture and validation target under `tests/spec/ING-010/`, plus redacted evidence under `docs/evidence/ING-010/<commit>/`. These are implementation destinations, not files already implemented by this specification.

## Detailed implementation steps

### Micro-task 1

Plan finance-first history and source-specific chunks; snapshot-only history starts at enrollment and unsupported periods are explicitly excluded.

### Micro-task 2

Reserve steady-state capacity; limit per-tenant/account active windows and adapt chunk size using measured extraction duration/bytes.

### Micro-task 3

After historical end T0, run overlapping catch-up to current source horizon; only then evaluate HEALTHY per source, not entire account blanket success.

### Micro-task 4 — Prove and checkpoint

Run the validation below, record expected versus observed results and environment versions, update operational documentation, then checkpoint the task index with the exact next action. Do not mark DONE while a required live gate is unexecuted.

## Data model

Backfill plan contains requested/available/missing ranges, priority, estimated rows/cost class, state and checkpoint.

## API / interfaces

POST /v1/sync/backfills returns plan; approve/start/pause/resume/cancel by revision.

## Security and tenant isolation

Extractors receive one account identity and prefix; sanitize before serialization; reject file/prefix/manifest identity mismatch. RAW and quarantine are not product-readable.

For this task, test a foreign tenant identifier and an invalid or revoked scope at every input boundary it introduces. Internal infrastructure tasks instead prove environment/principal isolation and absence of public access. Test artifacts use synthetic tenant A/B data only.

## Idempotency / retries

Immutable attempts plus stable logical window IDs, fenced leases, acceptance gates, deterministic keys and contiguous checkpoints provide idempotent business results over at-least-once transport.

## Failure scenarios

Customer pauses; rate limit; source retention expires during backfill; admission quota; catch-up never converges.

## Observability

Emit task/component, request/run/batch identifier, outcome, duration and a safe error class. Correlate tenant identifiers in protected logs, not unbounded public metric labels. Exclude credentials, full SQL and raw customer payloads. Record counts/checksums or configuration versions appropriate to this task; alert on repeated failures through the shared platform monitoring contract.

## UX behavior

This task introduces no direct customer page. Its outcome is surfaced through the linked domain capability, health or authorization contract. Emit safe progress/failure metadata; do not expose worker internals or silently render unavailable data as zero. Customer-facing follow-on tasks define their complete interaction states.

## Testing and validation

**Deterministic oracle:** 365-day request against 90-day source reports 90 available/275 unavailable; active customer incremental jobs retain capacity; resume does not restart accepted windows.

**Local:** the FND validation dispatcher must expose `make validate-task TASK=ING-010 ENV=local`. Implement the fixture before the behavior. Exercise the stated failure cases and retry the identical input; compare outputs with the oracle. Use unit/contract tests and local PostgreSQL/Redis integration where relevant. UI tasks add Playwright state assertions and keyboard checks; data tasks add dbt/Arrow contract assertions.

**AWS / Snowflake:** `make validate-task TASK=ING-010 ENV=staging` runs only against the isolated staging environment with short-lived identities. Exercise real Fargate→customer Snowflake→S3/Snowpipe→central RAW in staging; inject duplicate events, killed workers, missing files, late corrections and replay while comparing exact row/credit totals.

**Required test selection:** include the relevant unit, contract, integration, tenant-isolation, security, failure/recovery, E2E/UX, performance and financial levels. Record `NOT_APPLICABLE` with a concrete reason for any level that does not exercise this task. Never replace a specified live behavior with a mock and report it as passed.

## Acceptance criteria

- [ ] Every micro-task output exists at the named destination and agrees with the canonical contract.
- [ ] The deterministic oracle above passes with recorded expected/actual output.
- [ ] Stated failure and replay/retry cases preserve the last accepted state.
- [ ] Unauthorized/environment-crossing cases are denied and audited without leakage.
- [ ] UX and diagnostics represent unknown, partial and failed outcomes accurately.

## Definition of Done

All acceptance criteria plus the [global Definition of Done](../../../DELIVERY_METHODOLOGY.md) pass. Attach the implementation commit, versions, local and required live evidence, reviewer and rollback result. Update the machine task index; specification authoring alone leaves status NOT_STARTED.

## Operational runbook and rollback impact

Add diagnosis, safe retry, rollback and escalation instructions for the failures listed in this task. Record the last safe durable state; never delete evidence to clear a failure.

## Documentation impact

Update the [canonical domain contract](../../../docs/05-ingestion/ingestion.md), affected API/schema examples and task evidence index. Record any changed external assumption in the research register and any architecture change through the ADR process. Preserve one canonical definition rather than copying shared rules into route handlers or other task files.
