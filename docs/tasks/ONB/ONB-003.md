# ONB-003 — Prepare authorized first-customer environment and identities

## Hierarchy and execution metadata

Domain: ONB — Customer onboarding and first-value acceptance → Epic: E-ONB-01 → Feature: F-ONB-003 → User story: US-ONB-003 → Implementation task: ONB-003 → numbered micro-tasks below → validation → Definition of Done.

Milestone: M11. Owner persona: **Customer success / Snowflake / FinOps**. Implementation status: **NOT_STARTED**. Intended size: one focused implementation session; split into linked child tasks before coding if independent migrations/interfaces cannot fit safely.

## Objective / user story

As the responsible customer success / snowflake / finops, establish a real customer scope and accountable onboarding owners.

## Why this exists

This delivers **prepare authorized first-customer environment and identities** as a prerequisite-controlled capability. Its correctness is defined by the explicit oracle below, not by successful compilation. The [canonical contract](../../../docs/18-customer-onboarding/onboarding.md) owns shared definitions; do not invent parallel formulas or schemas.

## Dependencies

- [REL-004](../REL/REL-004.md)
- [ONB-002](../ONB/ONB-002.md)
- [LCH-001](../LCH/LCH-001.md)

Dependencies require accepted implementation evidence. A published prerequisite specification is insufficient.

## Inputs

Versioned synthetic fixtures and the accepted outputs of the explicit prerequisites below.

## Outputs and likely files/components

`docs/customer-records/onboarding-template.md`, `apps/api/tenant-provisioning`. Add a task-specific fixture and validation target under `tests/spec/ONB-003/`, plus redacted evidence under `docs/evidence/ONB-003/<commit>/`. These are implementation destinations, not files already implemented by this specification.

## Detailed implementation steps

### Micro-task 1

Confirm customer authorization, contract/plan, region, data classes, requested history, named admins and finance approver.

### Micro-task 2

Create tenant with scoped initial memberships and generate organization/account WIF installation package.

### Micro-task 3

Have customer execute reviewed scripts, verify identity/account matches and record capability evidence; refuse incorrect cross-customer binding.

### Micro-task 4 — Prove and checkpoint

Run the validation below, record expected versus observed results and environment versions, update operational documentation, then checkpoint the task index with the exact next action. Do not mark DONE while a required live gate is unexecuted.

## Data model

Private customer record with approved scope/region/retention/plan and identity references; no real customer data committed to public repository.

## API / interfaces

Provision tenant and initial admin via approved invitation workflow; installation package tied to configuration revision.

## Security and tenant isolation

Customer authorization and selected accounts govern collection. Invitations and ownership assignments are tenant scoped; setup scripts contain identities and grants but no persistent Snowflake secrets.

For this task, test a foreign tenant identifier and an invalid or revoked scope at every input boundary it introduces. Internal infrastructure tasks instead prove environment/principal isolation and absence of public access. Test artifacts use synthetic tenant A/B data only.

## Idempotency / retries

Every step is keyed by tenant/configuration revision; repeated starts reuse active work and accepted batches. Reconnection preserves durable account IDs and data lineage.

## Failure scenarios

Unapproved source; mistaken locator; unverified admin domain; unsupported residency; account already bound.

## Observability

Emit task/component, request/run/batch identifier, outcome, duration and a safe error class. Correlate tenant identifiers in protected logs, not unbounded public metric labels. Exclude credentials, full SQL and raw customer payloads. Record counts/checksums or configuration versions appropriate to this task; alert on repeated failures through the shared platform monitoring contract.

## UX behavior

| UX requirement | Behavior |
|---|---|
| Persona | Customer administrator / FinOps owner |
| Goal | establish a real customer scope and accountable onboarding owners. |
| Entry point | /onboarding/setup |
| Happy path | Review approved scope → invite admin → install WIF → validate accounts. |
| Empty state | Show exact information the administrator needs. |
| Loading state | Preserve selected scope and filters; show labelled skeletons and a cancellable progress indicator for asynchronous work. |
| Partial-data state | Explain optional denied capabilities before proceeding. |
| Error state | Keep user input; show a safe error code, retry action and support request ID. Never print SQL, tokens or internal hostnames. |
| Permission-denied state | Return a scoped 403 or non-enumerating 404; show an access-request explanation, with no foreign names, counts or totals. |
| Success state | Show the accepted version, UTC as-of time and completed outcome. Financial results include maturity, coverage and reconciliation status. |
| Drilldown behavior | Account → grant checks and remediation. |
| Primary actions | Invite, validate, approve scope, pause. |
| Acceptance criteria | Only approved accounts selectable; wrong Snowflake account identity fails validation; public repository contains template only. |

## Testing and validation

**Deterministic oracle:** Only approved accounts selectable; wrong Snowflake account identity fails validation; public repository contains template only.

**Local:** the FND validation dispatcher must expose `make validate-task TASK=ONB-003 ENV=local`. Implement the fixture before the behavior. Exercise the stated failure cases and retry the identical input; compare outputs with the oracle. Use unit/contract tests and local PostgreSQL/Redis integration where relevant. UI tasks add Playwright state assertions and keyboard checks; data tasks add dbt/Arrow contract assertions.

**AWS / Snowflake:** `make validate-task TASK=ONB-003 ENV=staging` runs only against the isolated staging environment with short-lived identities. Run rehearsal with a separate synthetic customer organization, then collect real customer evidence only after authorization; verify actual grants, history bounds, reconciliation and scoped UX.

**Required test selection:** include the relevant unit, contract, integration, tenant-isolation, security, failure/recovery, E2E/UX, performance and financial levels. Record `NOT_APPLICABLE` with a concrete reason for any level that does not exercise this task. Never replace a specified live behavior with a mock and report it as passed.

## Acceptance criteria

- [ ] Every micro-task output exists at the named destination and agrees with the canonical contract.
- [ ] The deterministic oracle above passes with recorded expected/actual output.
- [ ] Stated failure and replay/retry cases preserve the last accepted state.
- [ ] Unauthorized/environment-crossing cases are denied and audited without leakage.
- [ ] UX and diagnostics represent unknown, partial and failed outcomes accurately.

## Definition of Done

All acceptance criteria plus the [global Definition of Done](../../../DELIVERY_METHODOLOGY.md) pass. Attach the implementation commit, versions, local and required live evidence, reviewer and rollback result. Update the machine task index; specification authoring alone leaves status NOT_STARTED.

## Operational runbook and rollback impact

Add diagnosis, safe retry, rollback and escalation instructions for the failures listed in this task. Record the last safe durable state; never delete evidence to clear a failure.

## Documentation impact

Update the [canonical domain contract](../../../docs/18-customer-onboarding/onboarding.md), affected API/schema examples and task evidence index. Record any changed external assumption in the research register and any architecture change through the ADR process. Preserve one canonical definition rather than copying shared rules into route handlers or other task files.
