# ORC-004 — Integrate dbt assets and Python result dependencies

## Hierarchy and execution metadata

Domain: ORC — Dagster OSS orchestration and data publication → Epic: E-ORC-01 → Feature: F-ORC-004 → User story: US-ORC-004 → Implementation task: ORC-004 → numbered micro-tasks below → validation → Definition of Done.

Milestone: M3. Owner persona: **Data platform engineer**. Implementation status: **NOT_STARTED**. Intended size: one focused implementation session; split into linked child tasks before coding if independent migrations/interfaces cannot fit safely.

## Objective / user story

As the responsible data platform engineer, preserve transformation ownership and model-level lineage.

## Why this exists

This delivers **integrate dbt assets and python result dependencies** as a prerequisite-controlled capability. Its correctness is defined by the explicit oracle below, not by successful compilation. The [canonical contract](../../../docs/06-dagster/orchestration.md) owns shared definitions; do not invent parallel formulas or schemas.

## Dependencies

- [ORC-003](../ORC/ORC-003.md)
- [ING-009](../ING/ING-009.md)
- [CON-002](../CON/CON-002.md)

Dependencies require accepted implementation evidence. A published prerequisite specification is insufficient.

## Inputs

Versioned synthetic fixtures and the accepted outputs of the explicit prerequisites below.

## Outputs and likely files/components

`services/orchestrator/assets/dbt.py`, `services/orchestrator/assets/intelligence.py`. Add a task-specific fixture and validation target under `tests/spec/ORC-004/`, plus redacted evidence under `docs/evidence/ORC-004/<commit>/`. These are implementation destinations, not files already implemented by this specification.

## Detailed implementation steps

### Micro-task 1

Load the dbt manifest and map source assets/metadata without duplicating SQL in Dagster.

### Micro-task 2

Run algorithm stages only after declared inputs are accepted; persist immutable typed outputs before downstream marts.

### Micro-task 3

Fail one financial check and prove dependent publication/monitor/report assets do not execute while unrelated accepted scopes can proceed.

### Micro-task 4 — Prove and checkpoint

Run the validation below, record expected versus observed results and environment versions, update operational documentation, then checkpoint the task index with the exact next action. Do not mark DONE while a required live gate is unexecuted.

## Data model

Manifest/model unique IDs and Python algorithm_run_id with input publication/config version.

## API / interfaces

dbt CLI resource invokes Core; Python assets call versioned pure engines and persist results to Snowflake.

## Security and tenant isolation

Run configuration is server-derived and typed. Run tags and partition names are not authorization. Worker identities cannot choose other customer roles; UI is private and logs sanitized.

For this task, test a foreign tenant identifier and an invalid or revoked scope at every input boundary it introduces. Internal infrastructure tasks instead prove environment/principal isolation and absence of public access. Test artifacts use synthetic tenant A/B data only.

## Idempotency / retries

Dagster retries/sensor keys are hints; PostgreSQL fenced leases and source/publication keys enforce durable idempotency. Each publication pointer advances once per accepted candidate.

## Failure scenarios

Stale manifest/image mismatch; Python result exists only in memory; overly broad asset selection; config changes mid-run.

## Observability

Emit task/component, request/run/batch identifier, outcome, duration and a safe error class. Correlate tenant identifiers in protected logs, not unbounded public metric labels. Exclude credentials, full SQL and raw customer payloads. Record counts/checksums or configuration versions appropriate to this task; alert on repeated failures through the shared platform monitoring contract.

## UX behavior

This task introduces no direct customer page. Its outcome is surfaced through the linked domain capability, health or authorization contract. Emit safe progress/failure metadata; do not expose worker internals or silently render unavailable data as zero. Customer-facing follow-on tasks define their complete interaction states.

## Testing and validation

**Deterministic oracle:** No financial formula in Dagster definitions; dbt model lineage visible; failed required check blocks only the affected candidate publication.

**Local:** the FND validation dispatcher must expose `make validate-task TASK=ORC-004 ENV=local`. Implement the fixture before the behavior. Exercise the stated failure cases and retry the identical input; compare outputs with the oracle. Use unit/contract tests and local PostgreSQL/Redis integration where relevant. UI tasks add Playwright state assertions and keyboard checks; data tasks add dbt/Arrow contract assertions.

**AWS / Snowflake:** `make validate-task TASK=ORC-004 ENV=staging` runs only against the isolated staging environment with short-lived identities. Kill daemon and run workers in staging, duplicate sensor events, saturate backfills and verify role isolation, fair scheduling, accepted coverage and atomic publication.

**Required test selection:** include the relevant unit, contract, integration, tenant-isolation, security, failure/recovery, E2E/UX, performance and financial levels. Record `NOT_APPLICABLE` with a concrete reason for any level that does not exercise this task. Never replace a specified live behavior with a mock and report it as passed.

## Acceptance criteria

- [ ] Every micro-task output exists at the named destination and agrees with the canonical contract.
- [ ] The deterministic oracle above passes with recorded expected/actual output.
- [ ] Stated failure and replay/retry cases preserve the last accepted state.
- [ ] Unauthorized/environment-crossing cases are denied and audited without leakage.
- [ ] UX and diagnostics represent unknown, partial and failed outcomes accurately.

## Definition of Done

All acceptance criteria plus the [global Definition of Done](../../../DELIVERY_METHODOLOGY.md) pass. Attach the implementation commit, versions, local and required live evidence, reviewer and rollback result. Update the machine task index; specification authoring alone leaves status NOT_STARTED.

## Operational runbook and rollback impact

Add diagnosis, safe retry, rollback and escalation instructions for the failures listed in this task. Record the last safe durable state; never delete evidence to clear a failure.

## Documentation impact

Update the [canonical domain contract](../../../docs/06-dagster/orchestration.md), affected API/schema examples and task evidence index. Record any changed external assumption in the research register and any architecture change through the ADR process. Preserve one canonical definition rather than copying shared rules into route handlers or other task files.
