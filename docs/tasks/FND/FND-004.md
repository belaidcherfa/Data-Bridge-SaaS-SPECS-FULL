# FND-004 — Create golden synthetic tenant and finance fixtures

## Hierarchy and execution metadata

Domain: FND — Engineering foundation and local development → Epic: E-FND-01 → Feature: F-FND-004 → User story: US-FND-004 → Implementation task: FND-004 → numbered micro-tasks below → validation → Definition of Done.

Milestone: M0. Owner persona: **Tech lead**. Implementation status: **NOT_STARTED**. Intended size: one focused implementation session; split into linked child tasks before coding if independent migrations/interfaces cannot fit safely.

## Objective / user story

As the responsible tech lead, make tenant isolation and financial correctness testable before implementation.

## Why this exists

This delivers **create golden synthetic tenant and finance fixtures** as a prerequisite-controlled capability. Its correctness is defined by the explicit oracle below, not by successful compilation. The [canonical contract](../../../docs/01-architecture/engineering.md) owns shared definitions; do not invent parallel formulas or schemas.

## Dependencies

- [FND-003](../FND/FND-003.md)

Dependencies require accepted implementation evidence. A published prerequisite specification is insufficient.

## Inputs

Versioned synthetic fixtures and the accepted outputs of the explicit prerequisites below.

## Outputs and likely files/components

`data/fixtures/manifest.json`, `data/fixtures/finance`, `tests/fixtures`. Add a task-specific fixture and validation target under `tests/spec/FND-004/`, plus redacted evidence under `docs/evidence/FND-004/<commit>/`. These are implementation destinations, not files already implemented by this specification.

## Detailed implementation steps

### Micro-task 1

Create identities with colliding account-local query/resource IDs across tenants and overlapping same-tenant team scopes.

### Micro-task 2

Add warehouse 100 credits/70 attributed/30 idle, signed adjustments, missing prices, two currencies and malformed SQL cases. Store exact expected decimal strings.

### Micro-task 3

Generate deterministic duplicate, out-of-order, late correction and partial-batch variants from a fixed seed; prohibit real customer data in fixture scans.

### Micro-task 4 — Prove and checkpoint

Run the validation below, record expected versus observed results and environment versions, update operational documentation, then checkpoint the task index with the exact next action. Do not mark DONE while a required live gate is unexecuted.

## Data model

Fixture v1: tenant A/B, orgs, accounts, roles, source rows, expected totals and privacy cases; fixed UTC clock.

## API / interfaces

Fixture loader emits typed Arrow, PostgreSQL seed records and dbt seeds from one manifest.

## Security and tenant isolation

Use synthetic data locally; no production exports. Separate environment identities and redact tokens from commands and evidence.

For this task, test a foreign tenant identifier and an invalid or revoked scope at every input boundary it introduces. Internal infrastructure tasks instead prove environment/principal isolation and absence of public access. Test artifacts use synthetic tenant A/B data only.

## Idempotency / retries

Bootstrap is repeatable; dependency installers use locks. Fixture reseeding targets a disposable named database and refuses production endpoints.

## Failure scenarios

Timezone drift; binary float rounding; randomized output; fixture PII; accidental cross-tenant join masked by unique IDs.

## Observability

Emit task/component, request/run/batch identifier, outcome, duration and a safe error class. Correlate tenant identifiers in protected logs, not unbounded public metric labels. Exclude credentials, full SQL and raw customer payloads. Record counts/checksums or configuration versions appropriate to this task; alert on repeated failures through the shared platform monitoring contract.

## UX behavior

This task introduces no direct customer page. Its outcome is surfaced through the linked domain capability, health or authorization contract. Emit safe progress/failure metadata; do not expose worker internals or silently render unavailable data as zero. Customer-facing follow-on tasks define their complete interaction states.

## Testing and validation

**Deterministic oracle:** Regenerating fixtures produces identical checksums; both tenants have query Q1 without identity collision; 70+30=100 and not 200 credits.

**Local:** the FND validation dispatcher must expose `make validate-task TASK=FND-004 ENV=local`. Implement the fixture before the behavior. Exercise the stated failure cases and retry the identical input; compare outputs with the oracle. Use unit/contract tests and local PostgreSQL/Redis integration where relevant. UI tasks add Playwright state assertions and keyboard checks; data tasks add dbt/Arrow contract assertions.

**AWS / Snowflake:** `make validate-task TASK=FND-004 ENV=staging` runs only against the isolated staging environment with short-lived identities. Prove the selected version matrix in the staging AWS runner and record real connector/dbt/Dagster versions; local emulators do not prove cloud behavior.

**Required test selection:** include the relevant unit, contract, integration, tenant-isolation, security, failure/recovery, E2E/UX, performance and financial levels. Record `NOT_APPLICABLE` with a concrete reason for any level that does not exercise this task. Never replace a specified live behavior with a mock and report it as passed.

## Acceptance criteria

- [ ] Every micro-task output exists at the named destination and agrees with the canonical contract.
- [ ] The deterministic oracle above passes with recorded expected/actual output.
- [ ] Stated failure and replay/retry cases preserve the last accepted state.
- [ ] Unauthorized/environment-crossing cases are denied and audited without leakage.
- [ ] UX and diagnostics represent unknown, partial and failed outcomes accurately.

## Definition of Done

All acceptance criteria plus the [global Definition of Done](../../../DELIVERY_METHODOLOGY.md) pass. Attach the implementation commit, versions, local and required live evidence, reviewer and rollback result. Update the machine task index; specification authoring alone leaves status NOT_STARTED.

## Operational runbook and rollback impact

Add diagnosis, safe retry, rollback and escalation instructions for the failures listed in this task. Record the last safe durable state; never delete evidence to clear a failure.

## Documentation impact

Update the [canonical domain contract](../../../docs/01-architecture/engineering.md), affected API/schema examples and task evidence index. Record any changed external assumption in the research register and any architecture change through the ADR process. Preserve one canonical definition rather than copying shared rules into route handlers or other task files.
