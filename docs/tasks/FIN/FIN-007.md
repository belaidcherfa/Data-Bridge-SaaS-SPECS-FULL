# FIN-007 — Validate independent serverless service inclusion and totals

## Hierarchy and execution metadata

Domain: FIN — Canonical FinOps ledger, pricing and reconciliation → Epic: E-FIN-01 → Feature: F-FIN-007 → User story: US-FIN-007 → Implementation task: FIN-007 → numbered micro-tasks below → validation → Definition of Done.

Milestone: M4. Owner persona: **FinOps / Analytics engineer**. Implementation status: **NOT_STARTED**. Intended size: one focused implementation session; split into linked child tasks before coding if independent migrations/interfaces cannot fit safely.

## Objective / user story

As the responsible finops / analytics engineer, prove independently implemented serverless services form one complete non-overlapping financial view.

## Why this exists

This delivers **validate independent serverless service inclusion and totals** as a prerequisite-controlled capability. Its correctness is defined by the explicit oracle below, not by successful compilation. The [canonical contract](../../../docs/08-finops-ledger/ledger.md) owns shared definitions; do not invent parallel formulas or schemas.

## Dependencies

- [FIN-011](../FIN/FIN-011.md)
- [FIN-012](../FIN/FIN-012.md)
- [FIN-013](../FIN/FIN-013.md)
- [FIN-014](../FIN/FIN-014.md)
- [FIN-015](../FIN/FIN-015.md)
- [FIN-016](../FIN/FIN-016.md)
- [FIN-017](../FIN/FIN-017.md)

Dependencies require accepted implementation evidence. A published prerequisite specification is insufficient.

## Inputs

Versioned synthetic fixtures and the accepted outputs of the explicit prerequisites below.

## Outputs and likely files/components

`data/dbt/models/ledger/serverless`, `data/contracts/service-authority.json`. Add a task-specific fixture and validation target under `tests/spec/FIN-007/`, plus redacted evidence under `docs/evidence/FIN-007/<commit>/`. These are implementation destinations, not files already implemented by this specification.

## Detailed implementation steps

### Micro-task 1

Validate each service output against the shared charge schema and explicit authority mapping; no model may depend on another service ledger.

### Micro-task 2

Build the fixture union 5+7+2+2+2=18 and reject summary/detail double inclusion.

### Micro-task 3

Run each service model in isolation and then together, including unavailable detail capability and unknown billed resource.

### Micro-task 4 — Prove and checkpoint

Run the validation below, record expected versus observed results and environment versions, update operational documentation, then checkpoint the task index with the exact next action. Do not mark DONE while a required live gate is unexecuted.

## Data model

Independent models for pipe, streaming, tasks, alerts, clustering, search, MV and QAS; service/resource/time keys.

## API / interfaces

Each model can build from staging without another ledger model; typed unknown-service branch remains additive once.

## Security and tenant isolation

Financial reads honor tenant/account/group profiles; prices and close actions require FinOps permission. Source files and full query text are separately authorized. Never expose another account through org-level residuals.

For this task, test a foreign tenant identifier and an invalid or revoked scope at every input boundary it introduces. Internal infrastructure tasks instead prove environment/principal isolation and absence of public access. Test artifacts use synthetic tenant A/B data only.

## Idempotency / retries

Every charge/reference/version has a deterministic key; authoritative replacement is scope/version controlled. Closed artifacts are immutable; retries cannot create duplicate charge or adjustment entries.

## Failure scenarios

Old/new source overlap; unsupported preview; zero pipe ID inferred; malformed numeric string; new service silently dropped.

## Observability

Emit task/component, request/run/batch identifier, outcome, duration and a safe error class. Correlate tenant identifiers in protected logs, not unbounded public metric labels. Exclude credentials, full SQL and raw customer payloads. Record counts/checksums or configuration versions appropriate to this task; alert on repeated failures through the shared platform monitoring contract.

## UX behavior

This task introduces no direct customer page. Its outcome is surfaced through the linked domain capability, health or authorization contract. Emit safe progress/failure metadata; do not expose worker internals or silently render unavailable data as zero. Customer-facing follow-on tasks define their complete interaction states.

## Testing and validation

**Deterministic oracle:** Serverless total18 is unchanged by loading summary reference; optional detail denial leaves billed amount with reduced attribution coverage; QAS not billed twice.

**Local:** the FND validation dispatcher must expose `make validate-task TASK=FIN-007 ENV=local`. Implement the fixture before the behavior. Exercise the stated failure cases and retry the identical input; compare outputs with the oracle. Use unit/contract tests and local PostgreSQL/Redis integration where relevant. UI tasks add Playwright state assertions and keyboard checks; data tasks add dbt/Arrow contract assertions.

**AWS / Snowflake:** `make validate-task TASK=FIN-007 ENV=staging` runs only against the isolated staging environment with short-lived identities. Run golden dbt fixtures plus actual staging source reconciliation. Independently compare billing/statement evidence, not a ledger to the same table from which its amounts were copied.

**Required test selection:** include the relevant unit, contract, integration, tenant-isolation, security, failure/recovery, E2E/UX, performance and financial levels. Record `NOT_APPLICABLE` with a concrete reason for any level that does not exercise this task. Never replace a specified live behavior with a mock and report it as passed.

## Acceptance criteria

- [ ] Every micro-task output exists at the named destination and agrees with the canonical contract.
- [ ] The deterministic oracle above passes with recorded expected/actual output.
- [ ] Stated failure and replay/retry cases preserve the last accepted state.
- [ ] Unauthorized/environment-crossing cases are denied and audited without leakage.
- [ ] UX and diagnostics represent unknown, partial and failed outcomes accurately.

## Definition of Done

All acceptance criteria plus the [global Definition of Done](../../../DELIVERY_METHODOLOGY.md) pass. Attach the implementation commit, versions, local and required live evidence, reviewer and rollback result. Update the machine task index; specification authoring alone leaves status NOT_STARTED.

## Operational runbook and rollback impact

Add diagnosis, safe retry, rollback and escalation instructions for the failures listed in this task. Record the last safe durable state; never delete evidence to clear a failure.

## Documentation impact

Update the [canonical domain contract](../../../docs/08-finops-ledger/ledger.md), affected API/schema examples and task evidence index. Record any changed external assumption in the research register and any architecture change through the ADR process. Preserve one canonical definition rather than copying shared rules into route handlers or other task files.
