# CTL-006 — Implement scoped Redis keys and safe degradation

## Hierarchy and execution metadata

Domain: CTL — PostgreSQL control plane, transactional APIs and Redis → Epic: E-CTL-01 → Feature: F-CTL-006 → User story: US-CTL-006 → Implementation task: CTL-006 → numbered micro-tasks below → validation → Definition of Done.

Milestone: M1. Owner persona: **Backend**. Implementation status: **NOT_STARTED**. Intended size: one focused implementation session; split into linked child tasks before coding if independent migrations/interfaces cannot fit safely.

## Objective / user story

As the responsible backend, accelerate reads without weakening authorization or availability.

## Why this exists

This delivers **implement scoped redis keys and safe degradation** as a prerequisite-controlled capability. Its correctness is defined by the explicit oracle below, not by successful compilation. The [canonical contract](../../../docs/03-control-plane/control-plane.md) owns shared definitions; do not invent parallel formulas or schemas.

## Dependencies

- [CTL-004](../CTL/CTL-004.md)
- [SEC-006](../SEC/SEC-006.md)

Dependencies require accepted implementation evidence. A published prerequisite specification is insufficient.

## Inputs

Versioned synthetic fixtures and the accepted outputs of the explicit prerequisites below.

## Outputs and likely files/components

`packages/cache`, `services/query_broker/cache.py`, `tests/cache`. Add a task-specific fixture and validation target under `tests/spec/CTL-006/`, plus redacted evidence under `docs/evidence/CTL-006/<commit>/`. These are implementation destinations, not files already implemented by this specification.

## Detailed implementation steps

### Micro-task 1

Implement canonical filter hashing, per-dataset TTL and permission/dataset version keys; separate privacy modes/currency/timegrain.

### Micro-task 2

Add best-effort single-flight, jitter and bounded cold-query concurrency; cache outage falls back only within resource budget.

### Micro-task 3

Test tenant A/B, same-tenant profile differences, permission revoke, dataset swap, Redis flush and forced hash mismatch.

### Micro-task 4 — Prove and checkpoint

Run the validation below, record expected versus observed results and environment versions, update operational documentation, then checkpoint the task index with the exact next action. Do not mark DONE while a required live gate is unexecuted.

## Data model

Canonical request digest and full scoped key per contract; value includes dataset/as_of/coverage metadata.

## API / interfaces

get_or_compute requires authorized context and dataset version; no caller-supplied key.

## Security and tenant isolation

Apply SEC-004 policies to every new tenant table, validate composite FKs and object scopes in repositories. Logs and caches carry only safe scoped data.

For this task, test a foreign tenant identifier and an invalid or revoked scope at every input boundary it introduces. Internal infrastructure tasks instead prove environment/principal isolation and absence of public access. Test artifacts use synthetic tenant A/B data only.

## Idempotency / retries

Transactionally bind mutation, audit and outbox; unique idempotency keys and optimistic revisions prevent duplicate effects. Durable leases use fencing, not Redis locks.

## Failure scenarios

Cache stampede; stale entry; unavailable Redis; payload too large; browser retains prior tenant data.

## Observability

Emit task/component, request/run/batch identifier, outcome, duration and a safe error class. Correlate tenant identifiers in protected logs, not unbounded public metric labels. Exclude credentials, full SQL and raw customer payloads. Record counts/checksums or configuration versions appropriate to this task; alert on repeated failures through the shared platform monitoring contract.

## UX behavior

This task introduces no direct customer page. Its outcome is surfaced through the linked domain capability, health or authorization contract. Emit safe progress/failure metadata; do not expose worker internals or silently render unavailable data as zero. Customer-facing follow-on tasks define their complete interaction states.

## Testing and validation

**Deterministic oracle:** No cache hit crosses scope/version; Redis loss loses no job or permission state; 100 identical misses are bounded rather than 100 unrestricted queries.

**Local:** the FND validation dispatcher must expose `make validate-task TASK=CTL-006 ENV=local`. Implement the fixture before the behavior. Exercise the stated failure cases and retry the identical input; compare outputs with the oracle. Use unit/contract tests and local PostgreSQL/Redis integration where relevant. UI tasks add Playwright state assertions and keyboard checks; data tasks add dbt/Arrow contract assertions.

**AWS / Snowflake:** `make validate-task TASK=CTL-006 ENV=staging` runs only against the isolated staging environment with short-lived identities. Execute pooled RLS/transaction/failover tests on staging Aurora and Redis; verify Snowflake receives config versions once logically even when the outbox redelivers.

**Required test selection:** include the relevant unit, contract, integration, tenant-isolation, security, failure/recovery, E2E/UX, performance and financial levels. Record `NOT_APPLICABLE` with a concrete reason for any level that does not exercise this task. Never replace a specified live behavior with a mock and report it as passed.

## Acceptance criteria

- [ ] Every micro-task output exists at the named destination and agrees with the canonical contract.
- [ ] The deterministic oracle above passes with recorded expected/actual output.
- [ ] Stated failure and replay/retry cases preserve the last accepted state.
- [ ] Unauthorized/environment-crossing cases are denied and audited without leakage.
- [ ] UX and diagnostics represent unknown, partial and failed outcomes accurately.

## Definition of Done

All acceptance criteria plus the [global Definition of Done](../../../DELIVERY_METHODOLOGY.md) pass. Attach the implementation commit, versions, local and required live evidence, reviewer and rollback result. Update the machine task index; specification authoring alone leaves status NOT_STARTED.

## Operational runbook and rollback impact

Add diagnosis, safe retry, rollback and escalation instructions for the failures listed in this task. Record the last safe durable state; never delete evidence to clear a failure.

## Documentation impact

Update the [canonical domain contract](../../../docs/03-control-plane/control-plane.md), affected API/schema examples and task evidence index. Record any changed external assumption in the research register and any architecture change through the ADR process. Preserve one canonical definition rather than copying shared rules into route handlers or other task files.
