# ALC-004 — Implement usage group sets and effective hierarchies

## Hierarchy and execution metadata

Domain: ALC — External tags, allocation, usage groups, showback and chargeback → Epic: E-ALC-01 → Feature: F-ALC-004 → User story: US-ALC-004 → Implementation task: ALC-004 → numbered micro-tasks below → validation → Definition of Done.

Milestone: M6. Owner persona: **FinOps / Backend / Frontend**. Implementation status: **NOT_STARTED**. Intended size: one focused implementation session; split into linked child tasks before coding if independent migrations/interfaces cannot fit safely.

## Objective / user story

As the responsible finops / backend / frontend, support parallel business classifications without multiplying spend.

## Why this exists

This delivers **implement usage group sets and effective hierarchies** as a prerequisite-controlled capability. Its correctness is defined by the explicit oracle below, not by successful compilation. The [canonical contract](../../../docs/11-allocation/allocation.md) owns shared definitions; do not invent parallel formulas or schemas.

## Dependencies

- [ALC-003](../ALC/ALC-003.md)
- [CTL-003](../CTL/CTL-003.md)

Dependencies require accepted implementation evidence. A published prerequisite specification is insufficient.

## Inputs

Versioned synthetic fixtures and the accepted outputs of the explicit prerequisites below.

## Outputs and likely files/components

`apps/api/usage_groups`, `data/dbt/models/allocation/groups`, `apps/web/usage_groups`. Add a task-specific fixture and validation target under `tests/spec/ALC-004/`, plus redacted evidence under `docs/evidence/ALC-004/<commit>/`. These are implementation destinations, not files already implemented by this specification.

## Detailed implementation steps

### Micro-task 1

Create Teams/Products/Cost Centers/Environments sets from configuration, not separate code paths.

### Micro-task 2

Validate acyclic hierarchies, within-set assignment conservation and effective-date moves; preserve published prior trees.

### Micro-task 3

Test same200 charge across two sets, overlapping group membership and restricted-team viewer.

### Micro-task 4 — Prove and checkpoint

Run the validation below, record expected versus observed results and environment versions, update operational documentation, then checkpoint the task index with the exact next action. Do not mark DONE while a required live gate is unexecuted.

## Data model

Set/book ID, group parent/effective dates, membership/weights and explicit unassigned leaf.

## API / interfaces

CRUD /v1/usage-group-sets; hierarchy preview/publish; group-scoped views.

## Security and tenant isolation

Rule editors can target only their delegated scope. Simulation/results/group trees/statement URLs enforce tenant and group-book permissions. Publishing access-relevant classification changes requires permission review.

For this task, test a foreign tenant identifier and an invalid or revoked scope at every input boundary it introduces. Internal infrastructure tasks instead prove environment/principal isolation and absence of public access. Test artifacts use synthetic tenant A/B data only.

## Idempotency / retries

Versioned rule/config snapshots and deterministic allocation keys make reruns identical. Approval uses optimistic revision and input publication hash; issued statements remain immutable.

## Failure scenarios

Group deleted while used; cross-set parent; duplicated membership; orphan group; permission broadened by move.

## Observability

Emit task/component, request/run/batch identifier, outcome, duration and a safe error class. Correlate tenant identifiers in protected logs, not unbounded public metric labels. Exclude credentials, full SQL and raw customer payloads. Record counts/checksums or configuration versions appropriate to this task; alert on repeated failures through the shared platform monitoring contract.

## UX behavior

| UX requirement | Behavior |
|---|---|
| Persona | FinOps / organization administrator |
| Goal | support parallel business classifications without multiplying spend. |
| Entry point | /allocate/usage-groups |
| Happy path | Choose set → build hierarchy → simulate membership → publish. |
| Empty state | Create first set with explicit unassigned group. |
| Loading state | Preserve selected scope and filters; show labelled skeletons and a cancellable progress indicator for asynchronous work. |
| Partial-data state | Show unassigned and conflicting membership. |
| Error state | Keep user input; show a safe error code, retry action and support request ID. Never print SQL, tokens or internal hostnames. |
| Permission-denied state | Return a scoped 403 or non-enumerating 404; show an access-request explanation, with no foreign names, counts or totals. |
| Success state | Show the accepted version, UTC as-of time and completed outcome. Financial results include maturity, coverage and reconciliation status. |
| Drilldown behavior | Group → members/resources → charges and rules. |
| Primary actions | Create set/group, move, simulate, publish, inspect coverage. |
| Acceptance criteria | Each set totals200; combined400 is rejected as an invalid financial sum; hierarchy cycle cannot publish. |

## Testing and validation

**Deterministic oracle:** Each set totals200; combined400 is rejected as an invalid financial sum; hierarchy cycle cannot publish.

**Local:** the FND validation dispatcher must expose `make validate-task TASK=ALC-004 ENV=local`. Implement the fixture before the behavior. Exercise the stated failure cases and retry the identical input; compare outputs with the oracle. Use unit/contract tests and local PostgreSQL/Redis integration where relevant. UI tasks add Playwright state assertions and keyboard checks; data tasks add dbt/Arrow contract assertions.

**AWS / Snowflake:** `make validate-task TASK=ALC-004 ENV=staging` runs only against the isolated staging environment with short-lived identities. Run exact allocation fixtures in live dbt, including signed amounts, zero denominator, conflicts and two group sets; run UI/API cross-tenant and restricted-group tests.

**Required test selection:** include the relevant unit, contract, integration, tenant-isolation, security, failure/recovery, E2E/UX, performance and financial levels. Record `NOT_APPLICABLE` with a concrete reason for any level that does not exercise this task. Never replace a specified live behavior with a mock and report it as passed.

## Acceptance criteria

- [ ] Every micro-task output exists at the named destination and agrees with the canonical contract.
- [ ] The deterministic oracle above passes with recorded expected/actual output.
- [ ] Stated failure and replay/retry cases preserve the last accepted state.
- [ ] Unauthorized/environment-crossing cases are denied and audited without leakage.
- [ ] UX and diagnostics represent unknown, partial and failed outcomes accurately.

## Definition of Done

All acceptance criteria plus the [global Definition of Done](../../../DELIVERY_METHODOLOGY.md) pass. Attach the implementation commit, versions, local and required live evidence, reviewer and rollback result. Update the machine task index; specification authoring alone leaves status NOT_STARTED.

## Operational runbook and rollback impact

Add diagnosis, safe retry, rollback and escalation instructions for the failures listed in this task. Record the last safe durable state; never delete evidence to clear a failure.

## Documentation impact

Update the [canonical domain contract](../../../docs/11-allocation/allocation.md), affected API/schema examples and task evidence index. Record any changed external assumption in the research register and any architecture change through the ADR process. Preserve one canonical definition rather than copying shared rules into route handlers or other task files.
